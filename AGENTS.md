# AgentGuard 工作约定

## 固定 macOS App 身份

用户已明确要求：同一项目通常只保留一个主用 App，确需对照时最多再保留一个固定名称的对照 App。不得按任务、阶段、测试类型或构建轮次不断生成新名字。

- 当前唯一开发和原生验收入口：`apps/desktop-macos/src-tauri/target/debug/bundle/macos/AgentGuard Local Agent Test.app`。
- 固定 `productName` 和窗口标题为 `AgentGuard Local Agent Test`，Bundle ID 为 `com.agentguard.desktop.macos.localagenttest`，工作区配置为 `local-agent-20260910`。
- 本机继续使用已配置的组织签名身份；当前证书 SHA-1 为 `7C33B517942161311A087CE893D2F1CE9B204B44`。证书到期或更换必须明确记录，不能静默降为 ad-hoc。
- 所有本地功能、M1、M2、观察、回归均使用上述入口。旧 acceptance／M1／Observation／Recovery 命名只属于历史，不再生成新日常入口。正式外发构建另按发布计划执行，不作为本地测试改名的理由。
- 每次实际构建递增版本号，同时核对 App 版本、`CFBundleVersion` 和三语页脚；版本放在元数据和页面中，不追加到 App 文件名。
- 当前 App 为 2026-09-16 的 Ver 2.1（022）。原路径更新及同包重启后两项权限保留；本批实际复现并修复 Finder／Safari 输入框被误认为跨应用填写，普通安装正文为 LogOnly、合成注入仍产生 OVL-004 确认，详见 `docs/native-form-observation-022-2026-09-16.zh.md`。021 媒体、020 治理、018 活动时间、017 超时修复保持原证据身份；本批保留包内旧网关，只带入此前已提交的浏览器 CLI 清理修正。任意网页完整 AX 覆盖和真机超时恢复仍待验证。后续构建保持路径、标识与签名要求，并复核两项授权。
- 对照候选和历史证据保留可恢复归档，不留大量 `.app` 在用户应用目录或日常打包目录。归档前后核对文件、模式及链接，保留原路径映射；不得删除审计与失败证据。

## 本地构建与权限排查

- 授权状态与观察器运行状态分别核对。两项已授权但观察不完整时，先读取准确 App 的诊断错误；不得默认要求用户重绑权限。
- 当前 `local-agent-20260910` 开发工作区沿用带签名审计的 SQLite 格式，使用默认 `audit-sqlite` 特性。不得在日常故障修复中擅自切换存储格式或迁移历史；正式发布的 SQLCipher 门槛保持原要求。
- 本地打包前核对 `setup-assets` 的既有网关、浏览器安装资源和验收夹具，打包后逐项校验，不能把缺少资源的可启动 App 当作交付。扩展目录中的 `dist` 和 `_metadata` 为生成物，不应进入候选包。资源准备和核对完成后才使用固定证书签名。

## 本轮状态与范围

以开发计划及相应候选记录为准。F13 按用户要求暂缓、未验收；不能因此宣称完整原 M1 或发布通过。提交时只暂存本次明确修改的文件，保留原有独立成果和未跟踪文件。
