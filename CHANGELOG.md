[简体中文](CHANGELOG.md) | [繁體中文](CHANGELOG.zh-TW.md) | [English](CHANGELOG.en.md)

# 变更记录

本文件记录 AgentGuard 的重要变更。版本号遵循语义化版本。

## [未发布]

### 安装高风险截图复核（2026-09-16）

- 准确固定 App 021 已显示活动时间；两项权限保留，7 条新低对比度记录均为 LogOnly。5 项核心、2 项桌面及 133 项前端检查通过。Safari 合成安装文档可见但未取得 App 读取正文的证据，保留原生验收缺口；本轮未修改产品或重建 App，见[复核记录](docs/timeline-observation-018-2026-09-16.zh.md)。

### AGD-029 诈骗研究与模拟交易（2026-09-16）

- 完成两个诈骗候选手法、独立复核设计及深伪候选评估。真实受保护 Chromium 模拟流程 15 项通过，51 条审计与一笔正确本机账本记录对齐；没有运行深伪模型或产生真实付款，见[记录](docs/agd-029-fraud-review-2026-09-16.zh.md)。
- 修复浏览器清理程序在 macOS 沙箱中调用带提权位的 `ps` 导致 `EPERM`，并等待正常清理完成后再退出；保留目录及用户范围限制，四项生命周期与 30 项浏览器回归通过。
- 固定 App 021 的 91 个文件未变，本批修复属于外部网关源码候选。计划 v0.62：27 完成、1 附条件完成、1 开发中、3 待开始；性能、Safari AX、F13 与发布门槛继续保留。

### AGD-028 固定 App 媒体预览与保存（2026-09-16）

- 固定 App 同路径更新为 Ver 2.1（021），显示完整提取正文、来源、未覆盖内容及空白页；批准、拒绝与重开读取已实际操作，两项系统权限保持。独立核对 3 条签名、48 条审计，见[原生记录](docs/agd-028-native-media-2026-09-16.zh.md)。
- 发现并修正独立网关把压缩参数拼成超长词元的附带误报，保留正文、来源、明确注入检查及独立批准；固定 App 重新接入最终网关通过。新网关六格式回归、全仓 1,530 项，界面 17／16／133 项通过，保留首败和候选边界。
- AGD-028 按小范围接入要求完成，音视频另列 12～18 人日估算。计划 v0.61 为 26 完成、1 附条件完成、1 开发中、4 待开始；App 包内旧网关未替换，AGD-027 性能、F13 及发布门槛不变。

### AGD-028 安装资料误判与模型消费（2026-09-16）

- 实测发现正常安装文档虽已识别，持久保存仍被 CRIT-005 当作安装操作拒绝。仅在可信记忆入口排除基础安装文字规则，保存仍需独立批准；普通执行、自报字段、明确注入和附加策略保持约束，修正前失败保留。
- 两张图片经真实解析、保存和重开检索进入三个本机模型请求：正确读出安装示例及繁体金额，撤销后只得到无资料。独立核对 4 条签名、76 条审计，6 类篡改拒绝；新候选六格式回归、持久专项 7 项与全仓 1,528 项通过，见[验证记录](docs/agd-028-document-model-2026-09-16.zh.md)。
- 固定 App 020 未构建或更换身份；模型结果限脚本检索后消费，不是自主工具或原生验收。计划 v0.60 保持原任务计数，AGD-027 性能、App 接入及发布仍未验收，F13 暂缓。

### AGD-028 中文 OCR 与真实保存检索（2026-09-16）

- 图片解析改为离线、固定摘要的 PP-OCRv5，限制工作图和检测图尺寸；英文只在字符完全一致时复核空格，仍共用 15 秒截止。解析版本 2 保持历史版本 1 可读，不迁移记录。
- 原四张失败图与扩展共 13 图通过原 5% 门槛；真实 OCR 子进程暂停约 15.09 秒返回未知并回收。原 36 项组件、8 项入口、六格式网关及中文两进程保存／检索通过；原容器故障在新候选复验通过。内存峰值仍达到限额，OOM 和夹具首败保留，见[验收记录](docs/agd-028-chinese-ocr-2026-09-16.zh.md)。
- 全仓 1,525 项、SQLCipher 特性下文档专项 5 项及 Clippy 通过；中文证据独立核对 3 条签名、53 条审计，6 类报告篡改拒绝。固定 App 020 的 91 个文件与签名未变；前一提交 76150f9 的 CI 成功 13/13。计划 v0.59 保持原任务计数，模型／原生接入、AGD-027 性能及发布仍未验收，F13 暂缓。

### AGD-028 真实解析故障与中文 OCR 对照（2026-09-16）

- 两次真实解析容器故障均记为已派发、结果未知且不写记忆；暂停容器约 30.13 秒后由原执行截止清理，随后明确的新导入成功。空白 PDF 拒绝保存，部分 PDF 保留空白页说明，已测外部关系无观察到的副作用。
- 独立核对 5 条签名、69 条审计，7 类报告篡改拒绝。中文四张清晰图错误率 11.36%～15.91%；语言、版面及官方精度模型对照均未达到原 5% 门槛，保留失败且不替换产品镜像。见[验证记录](docs/agd-028-document-faults-2026-09-16.zh.md)。
- 上一批 8a7a70d 的 GitHub CI 成功 13/13。本批 App 020 未构建，名称与身份保持；计划 v0.58 仍为 25 完成、1 附条件完成、2 开发中、4 待开始，F13 暂缓及发布 No-Go 不变。

### AGD-028 隔离文档接入受控 RAG（2026-09-16）

- 六格式先核对路径并冻结原始字节，再在只挂单份输入的断网容器中解析；独立批准绑定正文、出处、解析器、镜像和覆盖缺口。原生模式与客户端自报解析结果均不能绕过。
- 新材料保持原始摘要与正文摘要分离，检索附提取行号和部件位置；四个真实 CLI 进程验证旧 MD 兼容、批准拒绝、跨重启撤销和保留原证明的恢复。独立注入文档触发 INTEL-INJECT 且零写入。
- 独立核对 11 条签名与 218 条审计，5 类报告篡改拒绝；全仓 1,524 项、网关库 244 项及开启 SQLCipher 特性的记忆 21 项通过。首败和验证脚本修正保留，见[网关记录](docs/agd-028-media-rag-2026-09-16.zh.md)。
- 9d9c833 CI 的 macOS 合成启动超时仍保留；去掉已复现的回环夹具反向 DNS 依赖，8 秒门槛未变。App 020 的 91 个文件与签名复核通过，未更新日常包。计划 v0.57 仍为 25 完成、1 附条件完成、2 开发中、4 待开始；中文 OCR、资源故障和原生接入继续推进，发布 No-Go 不变。

### AGD-028 隔离解析组件开发中（2026-09-16）

- 新增 PDF、DOCX、XLSX、PPTX、PNG、JPEG 的断网解析组件，分开绑定原始文件和提取正文摘要，列出读取层及未覆盖内容。宏／公式／外部关系不执行，空结果、依赖缺失、错误和超限不显示为安全。
- 基于旧镜像构建一个固定文档运行时，12 个依赖包逐项冻结，离线构建；36 项受限容器组件场景及 8 项实际入口通过。保留下载平台不匹配和像素超限误分类首败。
- 尚未接入受控 RAG 批准、来源／撤销和固定 App；中文 OCR、真实崩溃／超时及原生验证继续保留。见[组件记录](docs/agd-028-media-parser-2026-09-16.zh.md)。计划 v0.56 为 25 项完成、1 项附条件完成、2 项开发中、4 项待开始。
- AGD-027 另记录 36 个自有容器的生命周期诊断，性能门槛仍未通过；索引提交 fcc33de 的 GitHub CI 已完成 13/13。App 020 和现有默认镜像保持原状，F13 暂缓与发布 No-Go 不变。

### 审计序号索引与升级兼容（2026-09-16）

- 修正每次日志追加通过 `MAX(seq)` 扫描全部历史的问题；2,048 条记录时全表扫描从 2,047 次降为 0。旧签名、回执、序号及只读打开行为保持，兼容旧库缺失序号列。
- 原／新／原三个真实 CLI 进程的升级回退通过 51 项检查，二进制改变仍须独立审核；实际审计故障五场景另通过 51 项。全仓 1,518 项、SQLCipher 119 项和 Clippy 通过。
- 原预算四轮仍为 4/6、3/6、4/6、4/6，性能未验收；首败与全部结果保留。固定 App 020 未构建，时间线显示与签名再次核对。见[性能定位及证据](docs/agd-027-m3-performance-2026-09-16.zh.md)；计划 v0.55 仍为 25 项完成、1 项附条件完成、1 项开发中、5 项待开始，F13 和发布状态不变。

### AGD-027 联合验收进行中（2026-09-16）

- 新增真实四进程崩溃恢复与主体撤钥验收：已批准记忆保留、未批准写入不落库；旧密钥在正确新会话下被拒，新密钥对同一消息成功，移除主体后不能继续委托。
- 独立核对 49 条执行审计与 17 次签名结果，4 类证据篡改均拒绝；原 50 个完整任务通过。固定 App 020、91 个包文件和组织签名保持，未重新构建。
- 100 循环、原 30 分钟运行（30 任务／121 心跳）、浏览器 10 项／100 循环及 F14 虚拟链路复验通过；实际 SQLite 独立核对 1,225 条浏览器和 33 条 F14 审计，六类报告篡改拒绝。首轮性能仍为 4/6，整体验收未通过；主体撤钥要求停止旧宿主并重新加载配置。见[联合记录](docs/agd-027-m3-joint-2026-09-16.zh.md)，不改变 F13 与发布 No-Go。

- 修复 macOS 治理测试服务器继承非阻塞连接导致的 CI 间歇失败；分段正文在修复前复现同一错误，修复后完整桌面 134 项、仓库 23 项通过。仅修改测试夹具，固定 App 020 字节及签名不变。

### AGD-026 原生记忆与委托治理（2026-09-16）

- 新增来源与版本查看、隔离／撤销／历史恢复和委托分支停止；完整预览、单次批准、旧版本／会话拒绝，恢复保留来源和不可信标签，已完成外部动作不伪称撤回。
- 固定 App 更新为 Ver 2.1（020），修正切换连接残留旧条目数量。准确包两组真实原生操作通过，等待批准时停止 B 同时撤销 C，实际零写入；12 个签名、48 条执行审计独立核对。88 项资源保留，两项权限在更新和重启后保持。
- 三进程 CLI 另通过 24 次请求、7 个版本与 63 条审计；全仓 1,515 项、桌面 133 项、原工作区界面 133 项、治理界面 16 项及 SQLCipher 专项 16 项通过。新增治理界面 CI；全部首败与两批篡改拒绝证据保留。
- 更新[验收记录](docs/agd-026-memory-governance-2026-09-16.zh.md)、[证据索引](docs/evidence/m3-memory-governance-2026-09-16.json)、工作约定及计划 v0.52：25 项完成、1 项附条件完成、6 项待开始。限显式配置的本机网关；内置模型表单未自动启用记忆／委托，AGD-027、F13 和发布仍未通过。


- 020 源码提交 `37ce7dc` 的 GitHub CI 35033338484 最终 13/13 成功；包含新增治理界面回归和 Windows 窗口检查。

### 固定 App 018 活动时间与普通阅读误报（2026-09-16）

- 活动时间线显示记录的本地日期、时分秒和时区，保留 UTC 时间；缺失时间明确标记未知。
- 修正可信桌面观察中仅凭安装文字触发 CRIT-005、仅凭低对比纹理触发 OVL-007 的告警，改为有原因说明的普通记录。实际执行的安装批准、明确注入、付款及更严格策略保持原控制；历史审计不改写。
- 固定名称、路径、标识和组织签名不变，版本递增至 Ver 2.0（018）。88 项资源逐字一致、91 个包文件核对、两项权限保留；准确 App 已显示时间，新产生的 6 条 OVL-007 均为普通记录，628 条旧审计完整保留。
- 核心 268 项、桌面串行 129 项、前端 133 项、键盘语义回归、23 项仓库检查及 Clippy 通过。Safari 安装文档确实显示，但没有新 CRIT-005 审计，保留原生读取验证缺口。
- 保留本地并发测试超时和 AGD-025 云端 macOS 同类失败；仅将该 CI 作业的场景之间改为串行，原 8 秒门槛及场景内并发断言不变；018 提交 fbcfa17 的 GitHub CI 35027146897 已取得 13/13 成功。
- 再次核对高风险截图及准确 018 App：5 项观察专项通过，两项授权保留；实际 Safari 原 PR 可见，但没有新 CRIT-005 记录，完整原生读取链路仍未验收；OVL-009 告警也未包含在本次修复中。未构建新 App。
- 更新[018 修复记录](docs/timeline-observation-018-2026-09-16.zh.md)、[证据索引](docs/evidence/timeline-observation-018-2026-09-16.json)、三语能力矩阵和计划 v0.51。AGD-026 的隔离／恢复库测试 13 项通过，治理入口尚未交付，保持进行中；F13 仍暂缓未验收。

### AGD-025 持久投毒与复制传播验收（2026-09-16）

- 六个真实网关进程覆盖批准／拒绝写入、持久化重开、污染笔记与文档撤销、A→B→C 权限递减、两次实际文件投递及接收后的再次触发；正常读取继续可用，越权与超预算请求无新增副作用。
- 两次本机 35B 新任务分别验证资料可用和撤销后不再进入请求；模型建议不执行。传播请求由宿主驱动主动提交，收件目录仅为隔离测试文件，不扩大为真实收件人或自主传播实验。
- 独立重算通过 33 个签名、6 条记忆存储记录、162 条网关审计及共享预算；五类篡改证据拒绝。原委托完整流程／宿主回写和 23 项仓库检查通过。修复验收脚本可变引用导致历史动作被改写的问题，保留所有首败。
- 生产构建输入 201 项未变，复用既有网关与 Docker 镜像；固定 App 017 的 91 个文件和严格签名保持不变，本次未构建。017 提交的 GitHub CI 已成功（13/13）；跨应用 AX 读取和真机超时恢复缺口保留。
- 新增[验收记录](docs/agd-025-persistence-propagation-2026-09-16.zh.md)、[证据索引](docs/evidence/m3-persistence-propagation-2026-09-16.json)并更新计划 v0.50：24 项完成、1 项附条件完成、7 项待开始。F13 仍暂缓，整个计划和发布未通过。

### 固定 App 017 窗口观察恢复（2026-09-16）

- 修复已授权窗口观察因一次原生快照超时而永久停止的问题；只对已返回的快照超时等待 3 秒重试，未恢复前仍显示覆盖不完整，保留原生单飞及权限／审计故障停止边界。
- 分开提示权限缺失、已授权但观察不可用和恢复中；屏幕流停止不再一律误报缺少录屏权限。启动检查使用准确的“本地活动记录”文案。
- 固定路径、名称、标识和组织签名不变；017 更新及同包重启后两项权限保留，原 98 条审计保持不变。SQLite／SQLCipher 桌面库分别 129 项、前端 94 个断言及 Clippy 通过。
- 保留 016 特性／资源配置首败，未迁移原数据库；017 恢复既有资源和开发存储配置并严格复核签名。真实 SCK 观察已取得记录，其他应用 AX 内容读取和真机超时恢复尚未取得独立证明，不用界面绿色替代。
- 更新[017 修复记录](docs/native-observation-017-2026-09-16.zh.md)、证据索引、工作约定和计划 v0.49。AGD-024 提交的 GitHub CI 已成功；F13 仍暂缓、未验收，完整计划和发布状态不变。

### AGD-024 委托分支预算与父撤销（2026-09-16）

- 接通宿主预算、签名委托、独立操作者撤销、批准绑定、实际执行器及持久审计。全部祖先同锁扣减，兄弟共用总量；深度和单调期限逐级受限。金额不可可靠计量，明确由调用／输出／时间上限约束。
- 完整 JSON-RPC 响应计量包含转义、ID 和元数据；公开前审计失败、超时或撤销均隐藏正文并消费完整预留。修复审计失败提前释放额度的故障路径。父撤销取消等待，旧批准不能恢复，兄弟仍可继续；已完成效果不宣称回滚。
- 六个真实 CLI/Docker 进程覆盖深度、共享调用、输出、撤销、超时和真实 SQLite 写入故障。独立核对 13 份授权、33 个签名、16 次预留、113 条审计与零越权写入；四类篡改报告拒绝。原委托流程及宿主回写另完成两进程回归。
- 10 项预算测试、1 项实际 Docker 私钥检查、13 项 SQLCipher、全仓 1,506 项以及 Clippy／格式／矩阵检查通过，17 项忽略不计通过。保留错误接口脚本及能力矩阵未更新的首败，修复后重验。
- 更新[预算验收](docs/agd-024-delegation-budget-2026-09-16.zh.md)、[证据索引](docs/evidence/m3-delegation-budget-2026-09-16.json)、三语能力矩阵和计划 v0.48。原 127 文件与固定 015 App 的准确版本、摘要、签名证书保持一致；App 未重建。
- 当前 23 项完成、1 项附条件完成、8 项待开始；继续 AGD-025。F13 暂缓，完整原 M1、整个计划和发布仍未通过。

### AGD-023 Windows CI 测试范围修正（2026-09-16）

- 保留 `7db4ada` 的 Windows CI 首败：新增测试无条件初始化仅在 Unix 验证的执行日志锁。调整为各平台均检查超时 MCP 回执和零副作用，Unix 另核对持久审计，不改变生产平台限制。
- 本地超时专项及格式检查通过；修正提交 `2782d9d` 的 GitHub CI `35012604381` 已取得 13/13 成功，包含 Windows。生产委托实现、原冻结网关和验收证据保持原身份，详见[委托记录](docs/agd-023-delegation-2026-09-16.zh.md)。

### AGD-023 经认证主体的权限递减委托（2026-09-16）

- 新增真实隔离文件委托入口，绑定主体签名、父子会话、固定目标、逐级权限交集、期限与连续序号；同一 MCP 连接拒绝普通工具绕过。宿主配置、授权私钥及主体签名均在隔离任务外。
- 两进程真实 CLI 完成 A→B→C、读取／写入／删除、独立脚本批准、过期与重放拒绝、链接负例及实际宿主回写。独立核对 5 份授权、12 条认证消息、30 次负例、9 次文件动作、17 个签名和 71 条审计；三类篡改证据副本均拒绝。
- 复用既有 Docker 镜像，直接调用真实执行器核对四个宿主私钥文件均不可见。修复等待超时被 MCP 回执统一误报为普通拒绝的问题，回执与持久审计一致；新增零派发／零文件副作用回归。
- 10 项单元专项、1 项实际 Docker、13 项 SQLCipher 记忆回归、全仓 1,496 项、格式检查与 Clippy 通过；全仓 17 个忽略项不计通过，私钥专项另显式执行。保留编译、脚本路径假设及独立核对首败。
- 更新[委托说明](docs/agd-023-delegation-2026-09-16.zh.md)、[证据索引](docs/evidence/m3-delegation-2026-09-16.json)、三语能力矩阵与计划 v0.46。原 127 文件和固定 015 App 保持不变，准确 App 严格签名复核通过；基线 `e56695d` 的 CI 13/13 成功。
- 当前 22 项完成、1 项附条件完成、9 项待开始；下一项为分支预算与父撤销。F13 暂缓未验收，整个开发计划和发布未通过。

### AGD-022 网关记忆与文档检索（2026-09-16）

- 新增记忆保存／读取／撤销及 UTF-8 文档导入／关键词检索，接入已有会话、工具登记、任务计划、独立批准和执行审计。宿主明确配置存储路径、范围及读写能力，第三方内部记忆显示未覆盖。
- 检索保留原始文件快照、行号、出处、摘要、版本、标签与完整来源；先过滤实际资料键再限制结果数量。修复用工具名代替资料键的授权检查，最新版本过期或撤销后不回退旧版。
- 使用一个既有 Docker 镜像完成四个独立进程的真实 CLI 验收；四个获批版本、三次新模型任务通过，错误令牌、替换摘要、旧会话和越权零未授权写入。原文变化不替换批准快照，批准方式明确为脚本独立批准。
- 独立核对五条 Ed25519 签名、89 条审计与正文／来源绑定；对真实副本重算哈希链和外部见证也不能隐藏篡改。核心 4 项、网关 13 项、实际 SQLCipher 13 项、全仓 1,486 项和 Clippy 通过；16 个忽略项及全部首败记录保留。
- 更新[验收说明](docs/agd-022-memory-rag-2026-09-16.zh.md)、[证据索引](docs/evidence/m3-memory-rag-2026-09-16.json)、三语能力矩阵与计划 v0.45。原 127 文件和固定 015 App 身份、摘要、签名不变；前一提交 `23d6d43` 的 CI 13/13 成功。
- 当前 21 项完成、1 项附条件完成、10 项待开始；下一项为权限递减的委托契约。F13 暂缓未验收，整个计划和发布未通过。原生记忆治理仍归 AGD-026，不提前记为原生完成。

### AGD-021 受控持久记忆（2026-09-16）

- 新增真实 SQLite 记忆存储，绑定正文摘要、完整来源、标签、期限、版本前驱与单次批准；最新版本过期、隔离或撤销后不回落旧版本，恢复也不能降低标签。
- 每条记录签名，并与宿主持有的外部见证核对；修改正文／签名、重算链、删尾、搬库和修改普通批准字段均被拒绝。提交后见证失败明确返回未确认，停止使用，显式恢复只推进见证、不重放写入。
- 完成四个独立进程的落盘、重开、撤销与再次读取；保留篡改原件及最终验收程序，独立核对三条 Ed25519 签名、两版完整字段与批准摘要。真实 SQLCipher 加密和错误口令拒绝通过；合成明文及宿主合成批准单独标明。
- 相关测试 216 项、SQLCipher 测试 116 项、完整仓库 1,469 项及 Clippy 通过；16 个环境／子步骤忽略项不计通过。修复元数据读取上限，保留负例首败、早期编译／目录权限及矩阵未更新的原始失败。
- 更新[实现与验收记录](docs/agd-021-memory-store-2026-09-16.zh.md)、[证据索引](docs/evidence/m3-memory-store-2026-09-16.json)、三语能力矩阵与计划 v0.44。原 127 文件、固定 015 App 摘要及签名不变；前一提交 `269ab9e` 的 CI 13/13 成功。
- 当前 20 项完成、1 项附条件完成、11 项待开始；下一项是 AGD-022 的记忆读写和文档型 RAG。F13 暂缓，完整原 M1、整个计划和发布未通过。

### AGD-020 联合验收与原生生命周期（2026-09-16）

- AGD-020 按原任务范围完成：当前网关原定义 30 分钟、30 个完整任务与 121 次心跳通过；工作区 50 任务／100 循环、浏览器 10 任务／100 循环、签名规则 25 项与原性能预算 6/6 联合核对通过。
- 准确 015 App 实际完成未批准／执行中两种崩溃、只读、暂停恢复无重放和关窗清理；53 条新增审计与原有 30 任务／1,584 条记录核对，重开后辅助功能和屏幕录制仍已授权。固定名称、路径、标识与签名保持不变，本批没有重建 App。
- 复用既有合成容器补跑当前网关 F14：真实虚拟网卡断开／恢复后 25.23 秒无重发，新会话批准后才新增收件，48 条审计一致。本次明确为脚本批准，旧 010 原生批准记录保留其原身份。
- 新增规则／浏览器独立核对器，回归核对器兼容 012 和 015 并固定原性能预算；实际篡改副本不能用重算哈希链掩盖回写失败、错误来源或批准、缺项和账本变化。旧性能失败及核对脚本首次失败保留。
- 更新[联合报告](docs/agd-020-joint-regression-2026-09-16.zh.md)、[证据索引](docs/evidence/m2-joint-regression-2026-09-16.json)和计划 v0.43。原有 127 文件、两份 015 App 各 91 文件及旧 App 归档均保持不变；前一提交 `60a8f66` 的 CI 13/13 成功。
- 当前 19 项完成、1 项附条件完成、12 项待开始，下一项为受控持久记忆。F13 暂缓未验收，完整原 M1、整个计划和发布仍未通过。

### 固定 App 015、权限保留与原生修复任务（2026-09-15）

- 同一路径升级为 Ver 2.0（015），保持名称、标识和组织签名要求；两项权限更新后及同包重启后均保留，无需重新授权。修正权限就绪卡片、排查说明和采集停止后的历史消息标识。
- 界面交互 129 项通过；实际原生屏幕流启动／停止与停止后计数核对通过。新原生 35B 模型任务经过两次命令批准、两文件全文预览和实际回写，宿主原四项测试全部通过；原件恢复副本、新任务 54 条审计、旧 29 任务及 1,027 条历史记录核对通过。
- 015 复用 014 的同一网关字节，229 个生产输入不变。当前网关 50 个完整工作区任务与 100 次循环通过；DOM／真实模型共 950 条审计联合核对通过，旧格式正例及三类篡改负例通过。
- 前一提交 `b21b735` 的 GitHub CI 13/13 成功。原定义 30 分钟持续运行已启动，尚未计通过；AGD-020 及其余受影响回归继续。F13 暂缓未验收，完整计划与发布未通过。见[015 验收记录](docs/agd-020-native-015-2026-09-15.zh.md)。

### 014 候选、原预算复验与权限失配诊断（2026-09-15）

- 固定路径升级为 Ver 2.0（014）；013 因构建号残留 12 被拒绝并保留失败包，修正根配置后递增重建。014 严格签名通过，外发公证仍未通过。
- 准确包内网关的原性能预算 6/6，通过本机读取 p95 8.46 ms 和隔离读取 p95 255.20 ms；原五版失败与负载条件完整保留，不将一次测量扩大为全负载保证。
- 内容对照 20 项、审计故障 68 项、五类入口 52 项和真实 DOM 运行器 21 项通过；修正独立核对器对新共同来源日志的假设，393 条日志与 90 次越界拒绝核对通过，新旧正例及两类重算链篡改负例通过。
- 系统辅助功能开关已开但 App 不可用，`tccd` 确认旧授权的代码签名要求失配；移除旧条目后从固定路径重新添加，录屏也完成当前 App 授权。两项同包重启后保留，实际 AX 读取与原生屏幕帧、停止后状态已核对；桌面库 128 项通过。详见[014 记录](docs/agd-020-native-014-permissions-2026-09-15.zh.md)。
- 收拢日常目录 8 份旧 App 为可恢复归档，171 个文件／目录／链接条目核对不变，只保留当前已授权的固定开发入口。全局偏好与项目 AGENTS.md 固定名称、路径、标识和签名，对照最多一份。
- 基线 `0f3f41f` 的 CI 13/13 成功；AGD-020 保持实施中，F13 暂缓未验收，计划与发布未完成。

### 内容检测复用与开发构建优化（2026-09-15）

- 同次捕获的逐字相同内容复用检测结果；原始、可见与规范化视图的摘要、入口、完整性和上限保留。新增 96 组旧算法对照及可复用的真实双网关内容验收脚本。
- 根开发构建统一一级优化，JSON 与 SQLite 二级优化，保留 Rust 调试断言、溢出检查和原有持久化要求；独立桌面 workspace 不隐式继承。
- 完整仓库 1448 项、真实内容对照 20 项、审计故障与迁移 68 项、Docker 回写故障 8 项、桌面 128 项及 Clippy 通过；旧 127 文件和 012 两份 App 各 91 文件不变。
- 五个诊断版本性能均为 5/6，读取 p95 依次 11.55／12.05／12.01／10.21／20.72 ms；保留所有 450 个正式样本及 180 个观察器诊断样本，不改变原预算或基准脚本。最新测量伴随负载升高，因果未确认，性能仍不通过。
- 基线 `99d5f4c` 的 GitHub CI 13/13 成功；更新[本批记录](docs/agd-020-read-cpu-2026-09-15.zh.md)、结构化证据、三语能力矩阵及计划 v0.40。F13 暂缓未验收，AGD-020 继续实施，新 App 验收与发布未完成。

### 来源与执行终态共同提交（2026-09-15）

- 内建工具返回后，来源与终态共同持久保存，成功后才发布标签与正文；任一写入失败整对回滚，暂停当前会话，重启只补未知、不重放。读取正常路径由三次同步提交减为两次，持久化要求保留。
- 旧来源库只读留存并保持独占锁，完整验证来源图后一次导入与绑定；缺失或变化拒绝恢复，迁移末尾失败不留下部分导入。旧 4096 条恢复上限与界面快照上限分别保留。
- 完整仓库 1447 项、真实故障／迁移 7 场景共 68 项、Docker 回写故障 8 项、桌面 128 项及 Clippy 通过；旧 127 个独立文件和 012 两份 App 各 91 文件保持不变，严格签名通过。
- 原预算性能仍为 5/6：本机读取 p95 从 16.77 ms 降至 12.98 ms，未满足 10 ms；90 个正式样本正文正确。保留失败与同步落盘采样，未构建新 App。
- 前批 `83773ef` 的 CI 12/13 成功，macOS workspace 两项合成服务初始化超时；测试增加独立就绪检查，原协议期限及故障要求不变，新提交 CI 另行核对。更新[迁移记录](docs/agd-020-shared-audit-2026-09-15.zh.md)、结构化证据、三语能力矩阵和计划 v0.39；F13 暂缓未验收，AGD-020 继续实施。

### 网关摘要与审计事务修复（2026-09-15）

- ARM 网关启用运行时 SHA 硬件检测，并只优化开发构建中的算法库；完整可执行文件摘要与软件回退保留。
- 无需批准的判决与执行开始共享持久事务；迁移整体提交、失败回滚。修复单条追加未提前取得写锁的问题：加强测试后旧实现 50 次追加有 40 次失败，现要求全部成功、零漏记、链完整；持久化设置不降低。
- 完整 workspace 1438 项、Clippy、根及 macOS 供应链检查通过；真实 SQLite 故障 5 个场景／27 项和 Docker 回写故障 8 项通过。桌面 3 项启动等待曾超时，同一测试程序随后 128 项通过，原因未确认，保留两次结果。
- 最终诊断网关性能 5/6：功能测试之后启动 50.30 ms，读取 p95 16.77 ms 仍高于原 10 ms 预算；不是首次启动或新 App 验收。所有早期失败保留，012 固定路径和归档、127 个独立文件不变。
- 新增[真实审计故障运行器](scripts/acceptance/agd-audit-batching.py)，更新[修复记录](docs/agd-020-performance-2026-09-15.zh.md)、证据索引、三语能力矩阵及计划 v0.38。基线 `0f0299d` 的 CI 13/13 通过；AGD-020 继续实施，F13 暂缓未验收，完整计划及发布未通过。

### M2 候选回归与真实性能失败（2026-09-15）

- AGD-020 接续：原 30 分钟持续运行完成 30/30 任务、121 次心跳；50 项安全负例、78 项隔离、8 项显式故障、42 项宿主出口、24 次容器出口及浏览器故障回归通过。
- 固定路径 012 App 使用真实本地模型修复合成项目，经全文预览后实际回写；宿主原测试 3/3 通过，新任务 27 条审计验链、旧 28 任务及 993 条审计保持不变。
- 原预算性能只有 4/6：本机读取 p95 19.99 ms、启动 1316.54 ms 超标。保留正式失败与独立采样，定位软件 SHA-256 和同步审计热点，未降低门槛、未声称修复。
- 新增可复用容器出口运行器和回归证据复核器；预算篡改和审计篡改负例均被拒。本机桌面 128 项与仓库 23 项通过，012 App 及原有 127 文件未变。上一提交 CI 12/13 作业通过，3 项桌面启动等待超时本机未复现，重跑受仓库管理员权限限制。
- 更新[回归与失败记录](docs/agd-020-m1-regression-2026-09-15.zh.md)、结构化证据及计划 v0.37。AGD-020 保持实施中；F13 暂缓未验收，完整计划和发布未通过。

### M2 DOM、真实模型与签名规则独立验收（2026-09-15）

- AGD-020 接续：真实 Chromium DOM 21 项、本机已加载 35B 模型建议与权限边界 9 项通过；正常、已知攻击和新增组合分别统计，目录外动作与未经批准的提交均未产生副作用。
- 签名规则运行时 17 项、浏览器 8 项通过，独立核对实际文件、账本及 16 个已接受发布的签名。DOM／模型 948 条、规则联合 108 条审计验链通过；保留超长 DOM 节点重建错误，修正独立核对器后通过，审计与模型回答两种篡改副本均被拒。
- 原浏览器 B01～B10 与 100 次循环通过：50 次批准对应 50 次提交，40 次拒绝和 10 次断连取消均未提交。原定义 30 分钟持续运行已经启动，尚未计通过；其余 M1 回归继续，AGD-020 保持实施中。
- 仓库不变量 23 项与脚本／文档检查通过；更新[接续记录](docs/agd-020-dom-model-2026-09-15.zh.md)、结构化证据与计划 v0.36。上一提交 `42b20f8` 的 GitHub CI 13/13 通过；012 固定路径及归档 App、127 个独立文件保持不变，没有重建或发布 App。
- F13 暂缓未验收；累计 18 项完成、1 项附条件完成、1 项实施中、12 项待开始，完整计划和发布未通过。

### M2 五类资料入口与权限独立验收（2026-09-15）

- AGD-020 首批：冻结 9 份正常资料、已知攻击与新增组合变体，跨文件、搜索、容器进程、本地／远程 MCP 读取，加登记和恢复对照共 52 项通过。
- 独立核对 392 条审计及 90 个越界读写拒绝；伪造来源或裁判批准不清除来源、不获得执行权限。远端只有 9 次已批准读取、零写入效果，正常回写由宿主实际读回。
- 同一冻结网关的原工作区 T01～T50 与 100 次工作区循环全部通过，仓库不变量 23 项通过；独立核对器正常通过并拒绝篡改副本。DOM、真实模型裁判、签名规则联合与其余 M1 回归未完成，AGD-020 保持实施中。
- 保留三次证据导出失败与两次独立核对错误；显式指定可用 Python／SQLite 后重跑完整分母并导出通过。更新[验收记录](docs/agd-020-independent-inputs-2026-09-15.zh.md)、结构化证据和计划 v0.35。
- 上一提交 `73ed6cf` 的 GitHub CI 13/13 通过；012 App 及 127 个独立文件保持不变。当前 18 项完成、1 项附条件完成、1 项实施中、12 项待开始；F13 暂缓未验收，未发布安装包。

### 知识库与执行记录原生验收 Ver 2.0（012）（2026-09-15）

- AGD-019 按原计划功能范围完成：固定路径 App 实际验证知识库搜索、资料关联、四类执行记录、损坏通道及真正退出后重启读取。
- 新增本地验收构建配置，011 → 012 逐次递增，最终 `2.0.0-dev.12`；受控区域增加名称与焦点。自动化无障碍树仍漏报部分可见控件，保留待查，不记原生读屏通过。
- 桌面 128 项、记录页 18 项、原工作区 124 项、仓库约束 23 项及实际宿主导出 1 项通过；60 条日志与 21 个动作逐项核对。全部 28 个原始任务、993 条网关／浏览器审计前后不变。
- 010 完整归档、011／012 独立候选与 127 个原有文件全部核对；严格签名通过，未公证或发布安装包。上一提交 `f3301fe` 的 GitHub CI 13/13 通过。
- 更新[原生记录](docs/agd-019-native-workspace-2026-09-15.zh.md)、结构化证据与计划 v0.34，保留原始失败。当前 18 项完成、1 项附条件完成、13 项待开始；F13 暂缓未验收，完整计划和发布仍未通过。

### 受控执行记录与实际阶段证据（2026-09-15）

- AGD-019 第二批：活动页读取本应用任务网关与浏览器日志，显示实际判决、开始和终态，区分告警、阻断、未知与执行记录；工具返回保留业务效果未核对。
- 完整快照只读验链，核对动作、策略和批准绑定；缺失、损坏或超限不冒充完整历史。规则编号只关联参考资料，不据此认定攻击阶段发生。
- 全仓 1433 项、桌面库 128 项、新记录页 17 项、原工作区 124 项和知识库 21 项通过；新一轮真实 Docker／MCP／HTTPS 17 项、浏览器 8 项、故障 6 项通过。独立验链并核对 84 条记录、32 个展示动作。
- 更新[执行证据记录](docs/agd-019-execution-evidence-2026-09-15.zh.md)、结构化证据与计划 v0.33，保留测试夹具失败和修正过程。AGD-019 仍差原生 App 验收；010 App 的 90 文件与签名、127 个独立文件均保持一致。
- 上一提交 `81ad48d` 的 GitHub CI 13/13 通过。本批未发布安装包；F13 暂缓未验收，完整计划保持 17 项完成、1 项附条件完成、1 项实施中、13 项待开始。

### 知识库工作区与观察记录分类（2026-09-15）

- AGD-019 第一批：现有工作区接入固定内嵌知识库，支持手法／案例／规则搜索、阶段资料关联、覆盖登记和验收证据查看；全部阶段保留实际发生未知，12 个未运行场景不升级为通过。
- 活动页区分观察告警、未知与普通记录；本机观察接口没有执行回执，不能把 Block 或用户拒绝计作已阻断。资料正文和来源网址只作文本，不获得执行或导航权限。
- 3 项 Rust 专项、桌面库 124 项、知识库 Chromium 21 项、原界面 124 项、仓库约束 23 项及严格 Clippy 通过。浏览器验证使用真实命令数据与 IPC 测试桥，不冒充原生验收。
- 更新[实施记录](docs/agd-019-knowledge-workspace-2026-09-15.zh.md)、证据和计划 v0.32。AGD-019 保持实施中，受控执行证据和原生验收继续推进。010 App 与 127 个独立文件未变；F13 暂缓未验收，未发布安装包。
- 上一提交 `1907a1c` 的 GitHub CI 13/13 通过。全计划为 17 项已完成、1 项附条件完成、1 项实施中、13 项待开始。


### 生产网关签名规则包切换（2026-09-15）

- AGD-018 完成 CLI 范围：签名规则包作为附加约束接入内建工具、本地／远程 MCP、宿主回写、浏览器 DOM／HTTP；更新保留会话与预算，旧批准不能跨发布代次执行，恢复相同包也不复活旧批准。
- 动作期间持有 SQLite 事务，更新明确等待或失败；修正只读状态查询争抢写锁。每笔执行审计保存包摘要与发布序号，正文与宿主路径不直接落盘。
- 新增 8 项组件测试、25 项真实生产入口实操；全仓 1423 项、桌面库 122 项、原本地 MCP 21 项、远程联合 33 项及浏览器 2 项回归通过。独立核对 16 条签名、108 条审计及其中全部 80 条动作策略绑定。
- 更新[生产接线记录](docs/agd-018-runtime-rule-policy-2026-09-15.zh.md)、结构化证据和计划 v0.31；保留全部失败与修正证据。F13 仍暂缓，010 App 文件和签名保持，未发布安装包。全计划当前 17 项已完成、1 项附条件完成、14 项待开始。

### 签名包格式、持久撤销与管理 CLI（2026-09-15）

- AGD-018 开始实现：知识包与规则包严格分型，Ed25519 覆盖兼容协议、序号、安全代次、灰度及撤销／恢复目标；拒绝重复字段、未知内容和签名降级。
- 新包仓库通过 SQLite 事务保存签名流水，重启和并发更新重新核查；旧序号、低版本及低安全代次不能覆盖当前状态，更新失败保留有效包，撤销后不隐式回退。
- 实际 `guard-cli package` 提供签发、检查、初始化、应用、查询和引擎判决；新增 30 项组件测试、20 项实际 CLI 检查、全仓 1415 项及桌面 122 项通过，独立核对 7 条已接受签名。保留两次严格 Clippy 失败及修正记录。
- 更新[包生命周期记录](docs/agd-018-package-lifecycle-2026-09-15.zh.md)、证据及计划 v0.30；生产网关策略切换、来源和旧批准失效仍待完成，AGD-018 不计完成。F13 继续暂缓，010 App 未构建替换。
- 上一 AGD-017 提交 `96b7285` 的 GitHub CI 13/13 通过；当前提交 CI 独立跟进。

### 有限远程 MCP 产品接线与联合验收（2026-09-15）

- 远程服务接入生产 CLI 配置、实际清单登记、逐次批准、网络判决、完整返回来源和持久未知恢复；发现令牌与调用令牌分离，连接配置摘要明确不等于远端代码证明。
- 修复派发锁内取消回调重入导致远程卡住的问题；撤权锁和令牌校验保持，旧实现失败及修复后真实 TLS 回归均保留。
- 33 项联合实操、21 项旧本地回归、10 项真实 TLS、全仓 1385 项和桌面 122 项通过；独立核对两端文件及 143 条审计链。实测 30 秒超时、已写入断连读回、暂停后远端仍继续及 SIGKILL 恢复，不自动重发。
- 更新[联合记录](docs/agd-017-remote-product-2026-09-15.zh.md)、证据及计划 v0.29，AGD-017 按限定范围完成，接续 AGD-018。F13 仍暂缓未验收；准确 010 App 的 90 文件及签名复核通过，未打包替换。
- 上一传输提交 `91ff26e` 的 GitHub CI 13/13 已通过；当前提交 CI 独立跟进。

### 远程 MCP 传输及令牌边界（2026-09-15）

- 固定 MCP 2025-06-18，支持有界 HTTPS JSON／SSE，精确地址与 TLS 名称校验；预配签名令牌验证受众、范围和期限，实际连接与写入受撤权锁约束，失败后不重发。
- 全仓 1381 项、桌面 122 项及三套依赖检查通过；新增 7 项单元、9 项真实 TLS 测试及独立 Node HTTPS 3 项互通检查；修正 macOS 测试夹具继承非阻塞标记导致的首轮断连，保留失败日志。
- [远程传输记录](docs/agd-017-remote-transport-2026-09-15.zh.md)及计划 v0.28 同步更新。远程产品路由与两类服务联合验收尚待接线，AGD-017 仍进行中；F13 暂缓，010 App 未替换。
- 上一本地代理提交 `5b9fe62` 的 GitHub CI 13/13 已通过，当前提交 CI 单独核对。

### MCP 本地产品路由与重启恢复（2026-09-15）

- 宿主配置的实际服务经发现和独立登记后发布工具；每次调用绑定包、挂载、会话、参数及工具代次并单独批准，实际启动和管道写入与撤权共用锁。
- 服务初始化也纳入批准；结果进入内容来源检测，副本变化仍需独立预览回写。新增私有恢复索引，重启核对精确容器身份后清理，原动作记未知、不重放。
- 标准 JSON Schema 校验禁用文件与网络获取，明确拒绝当前未支持的引用和方言。根与壳子仅为新增精确依赖 `borrow-or-share 0.2.4` 补充经核对的 MIT-0 许可项。
- [21 项实际产品检查及故障记录](docs/agd-017-local-proxy-2026-09-15.zh.md)通过；保留初次故障注入表名错误和既有桌面间歇超时。F13 暂缓，010 App 未打包或替换，AGD-017 的远程部分及完整计划继续推进。
- 上一发现提交 `6b2da4c` 的 GitHub CI 13/13 通过；当前提交的 CI 单独核对。

### MCP 完整发现与运行身份登记（2026-09-15）

- 完整握手、工具元数据与实际执行摘要进入登记和独立复核；旧内建清单编码保持不变。标题、输出定义、注解、元数据及启动身份变化使旧认可失效，下游自报内容不能覆盖宿主回执。
- 官方 Filesystem 服务完成 8 项发现登记实操；修正超时后删除命令报错时缺少实际容器状态核对的问题，继续保留清理未知的故障关闭和原始失败证据。
- 全仓 1352 项、桌面壳子 122 项、原有 14 项隔离实操及严格检查通过；新增 9 项默认组件测试，未打包或替换 App。
- 更新[发现登记记录](docs/agd-017-discovery-registration-2026-09-15.zh.md)；完整代理及后续计划仍进行中，F13 暂缓、010 App 保持不变。上一隔离提交 `d369386` 的 GitHub CI 13/13 通过。

### 第三方服务的包冻结与隔离启动（2026-09-15）

- 增加完整包字节和运行身份绑定；工具发现使用只读包和空工作区，授权运行只接既有隔离副本，保持宿主独立回写批准。
- 启动前核对内核约束；超时删除并复查容器，清理未知时拒绝新操作并保留冻结包。原包替换、网络／环境边界、子进程停止及三个内核配置负例均已实际验证。
- 最终 14 项容器检查、新增 9 项组件测试、全仓 1343 项及严格检查通过；保留临时目录和栈溢出原始失败。更新[隔离记录](docs/agd-017-service-isolation-2026-09-15.zh.md)及计划 v0.25；完整 AGD-017 仍进行中，F13 暂缓，010 App 不变。

### macOS 合成模型测试的失败诊断（2026-09-15）

- stdio 组件首轮提交的 CI 12/13 通过，macOS 壳子中 3 个既有测试等待模型请求超时；保留失败，本地壳子 122 项复验通过。
- 超时信息加入预期路径及实际运行阶段，保留 8 秒门槛和原行为断言，继续定位远端原因；不将诊断改进称为根因修复。

### 第三方 MCP 的 stdio 传输组件（2026-09-15）

- AGD-017 开始实现：限定 2025-06-18 的非阻塞 stdio 通道、消息上限、总时限、严格 JSON、断连和取消处理；故障后不重发。登记批准与产品路由仍未接线。
- 8 项组件测试、全仓 1334 项和官方 Filesystem 服务 7 项真实 Docker 检查通过，保留夹具及严格 Clippy 失败；未替换准确 010 App。[代理接续记录](docs/agd-017-mcp-proxy-2026-09-15.zh.md)与计划 v0.24 同步更新。
- AGD-016 的最新基线 `ac102f2` 已完成 GitHub CI 13/13；F13 仍暂缓未验收，完整计划与发布未完成。

### 工具登记、包冻结与变更复核（2026-09-15）

- 完成 AGD-016：工具服务、名称空间、包身份与清单摘要进入独立登记；首次外部观测不自动认可，变更与撤销使旧动作批准失效。
- 浏览器资源启动前核对，从同一批核验字节的冻结副本运行；修复撤销与 HTTP 派发的锁顺序，保留原始失败。
- 全仓 1326 项、桌面 122 项、真实宿主 71 项检查通过；来源／内容回归通过，62 条审计验链。更新[工具登记记录](docs/agd-016-tool-registry-2026-09-15.zh.md)与计划 v0.23。F13 仍暂缓未验收，010 App 不变；接续 AGD-017。

- 首轮 CI 后补正 Windows 并发测试夹具，并将三个锁文件中的 rustls 升级到 0.23.45，修复 RUSTSEC-2026-0285；原失败与复验分开保留。
- 进一步限定浏览器包冻结模块的编译平台，修复 Linux 严格 Clippy；保留所有平台的单元测试。

### 原始内容、可见内容与检测视图（2026-09-14）

- 完成 AGD-015：文件读取／搜索、DOM 文本节点与进程双输出流在实际读取时采集，原始、可见、检测视图分别摘要；私有原文不进入模型或审计。
- 修复隐藏节点拼接漏检和中文截断前缀的检测上限，保留原失败；正常 Unicode 与研究文档可读，读取内容没有批准权。
- 全仓 1310 项、桌面 122 项、Docker 43 项和来源回归 15 项、真实浏览器 9 项通过；更新[内容视图记录](docs/agd-015-content-views-2026-09-14.zh.md)与计划 v0.21。F13 仍暂缓未验收，未替换 010 App；接续 AGD-016 工具登记。


### 来源持久恢复与真实工具输出接线（2026-09-14）

- 完成 AGD-014：来源日志独占写入、重启恢复与父标签校验；工具、浏览器和宿主工作区动作共用来源绑定，模型自报可信不能授予权限。
- 来源故障停止后续动作，不回退到内存成功。Docker 实操 15 项、真实 Chromium 联合路径、全仓 1302 项及桌面 122 项通过；[证据与失败记录](docs/agd-014-source-provenance-2026-09-14.zh.md)已更新。
- 接续 AGD-015 原始内容与检测视图；本轮摘要只代表工具实际返回。F13 仍暂缓未验收，未替换 010 App，完整原 M1 和发布仍未通过。

### F13 暂缓、F14 网络恢复与来源标记首批（2026-09-14）

- 用户明确暂缓 F13，保留未验收；F14 经准确 010 App 原生批准、Docker 实际虚拟网卡断开／恢复、未知回执及 25 秒无重发验证，新会话只提交一次；33 条审计验链、90 个 App 文件复核通过。
- AGD-014 开始实现来源敏感度、保守继承、宿主采集器和脱敏审计；三类读取入口尚未接线，不计整项完成。
- 同步开发计划、验收例外及[网络记录](docs/agd-f14-docker-network-2026-09-14.zh.md)、[来源记录](docs/agd-014-source-provenance-2026-09-14.zh.md)。旧失败保留，完整原 M1 和发布结论不因暂缓自动通过。

### 受保护本地 Agent 与 M0／M1 源码整合（2026-09-14）

- 整合本地模型、受控工作区副本、命令及全文差异批准、宿主回写、恢复原件、持久审计和专用浏览器会话；保留默认只读与明确数据授权。
- HTTP 待批准时等待终态；允许编辑新的表单草稿，但旧请求正文及单次批准绑定不变。拒绝、超时和未知结果不自动重试。
- 纳入威胁知识库、执行契约、隔离及正常任务验收脚本，以及 macOS 观察告警和网关可用性修复。
- Ver 1.0（010）已有冻结后台 60/60 任务、30 分钟运行，以及记录失败和明确追加后的原生逐项补验；宿主 3/3 测试、浏览器单次提交和 F07 两阶段通过。不能视作模型一次自主联合任务成功。
- 本次合并验证与原始日志位置见[源码整合记录](docs/agentguard-source-merge-2026-09-14.zh.md)。M1 仍缺 F13 实际睡眠／唤醒和 F14 非回环断网恢复；计划保持 12 项完成、1 项阻塞、19 项待开始，生产发布仍为 No-Go。
- 修复合并后 CI 暴露的 Linux Clippy 与场景登记遗漏、浏览器测试关页异常处理、Windows 未支持日志锁的测试假设，以及 macOS 长输出读取的额外等待；失败与复验记录保留。
- 固定知识库夹具为 LF，避免 Windows 检出改变登记摘要；CI 精确启用自建网关副作用测试，人工宿主连接验收保留独立运行条件。

### 实验性网页邮箱防护（2026-09-07）

- 在 Chrome/Edge 扩展新增默认关闭的独立邮件设置、Gmail/Outlook 候选 DOM 检查、敏感主题/正文及收件人核对、阅读伪指令与链接错配检测；未实现附件扫描，已识别附件动作保守阻断。
- 新增原样扩展的 Chromium 本地邮件验收及最小化日志检查；直接 API 可绕过作为已知缺口明确记录。真实邮箱与 Edge 尚未验收，代理和批准投递未实现，不作为正式发布或全覆盖声明。

### 邮件联合防护设计（2026-09-06）

- 新增三语[设计方案](docs/mail-protection-design.md)，覆盖企业收发网关、网页与客户端接入、权限和隐私边界、发送批准绑定及邮件专用验收集。
- 本轮只完成设计与本地交互预览检查；尚未实现邮箱代理、连接真实账号或改变邮件路由，不将现有通用测试计为邮件防护通过。

### CI 跨平台回归修复（2026-09-06）

- 能力矩阵脚本固定 UTF-8 输出，并在仓库测试中强制复现 Windows 的 cp1252 管道环境；macOS 适配器的非原生分支正确限定平台专属代码，保持严格编译警告检查。
- webhook 冒烟改用运行时生成的临时事件，真实 CLI 验证有效、重试、过期、未来、乱序、缺字段及退款场景；不修改历史夹具，不放宽十分钟时间窗。
- 签名流水线先验证缺少身份时默认发布路径拒绝，再显式启用 ad-hoc 模式验证临时替身；不降低正式签名、公证或发布门槛。
- Windows 打包测试兼容检出后的 CRLF 换行，仍要求正式密钥使用 DPAPI，不允许明文签名回退。
- Windows 加密审计 CI 显式核对并连接 runner 预装的 x64 MSVC OpenSSL 头文件、链接库与运行时；缺失即失败，不跳过 SQLCipher 测试。

### 跨平台整改源码整合（2026-09-06）

- 整合共享核心、加密审计与确认生命周期，以及桌面、移动端、浏览器扩展的既有整改；新增三语[提交说明](docs/remediation-publication-2026-09-06.md)，明确验证范围与未完成门槛。
- CI 补入工作区界面回归及真实 MCP 网关文件副作用测试；同步权限声明、证明测试映射与生成能力矩阵。
- 源码审查候选随后按维护者要求通过仓库 SSH 身份合入 `main`，收敛远端与本地分支；排除本机凭据、原始报告与构建产物。不发布安装包，生产仍为 No-Go。

### macOS 工作区与主动防护入口（2026-09-06）

- 补齐受认证的本机网关待确认列表、当前请求的拒绝/勾选后批准、倒计时与断连保护；令牌仅临时保留，不保存或自动重连。首次应答生效，过期和重复回答被拒绝；真实临时文件验证拒绝无副作用、批准后才写入。
- 新增总览、主动防护、活动记录、防护设置、帮助与诊断；设置按四个标签组织，提供本地保存的语言、外观和下一会话任务。
- 必需辅助功能与可选录屏分开；当前 App 文件名及路径可核对，重新检测及状态失败有反馈，后端也拒绝未授权的桌面启动。
- 随包提供 Chrome/Edge 测试扩展、无害验收页及当前平台 MCP 网关；向导可实际握手并生成不含令牌、不默认扩大工作目录权限的配置。
- 验收 App 使用独立固定名称/标识；ad-hoc 重编译仍需重新检查权限。指定客户端接入闭环、正式签名与发布验收尚未完成；本轮不宣称跨平台改版或可上线。
- 同步三语桌面使用说明，纠正“桌面提醒等于事前拦截”和无认证网关确认示例。

### 真机报告（2026-08-31，含 2026-09-02 Windows 补充）整改

按报告的 P0 / P1 / P2 编号逐项处理，每项带红-绿-突变测试；只能在真机、证书或商店账号上发生的部分仍标为未验证，见 [docs/release-evidence.md](docs/release-evidence.md)。

- **P0-1 门禁伪证**：发布证据改为十二类结构化校验（绑定完整提交号、真实命令、退出码、判据输出与产物身份）；新增 iOS Apple Distribution、iOS 真机、TestFlight、Chrome 与 Edge 独立 fail-closed 门禁，只含关键词的文件一律拒。
- **RC / GA 分层门禁**：`--strict` 固定为 12 类 RC 技术证据；`--ga` 在此基础上再要求 SBOM/许可证、隐私/商店声明、14 天 Beta、双 RC、五方签字、六渠道 smoke 与 5%→25%→100% 扩量，共 19 类。首个 GA 排除 Firefox；当前真实 GA 闭环材料缺失，结论仍为 No-Go。
- **P0-2 CI 两处根因**：Windows 扩展路径降级、Landlock `prctl` 尾参数残值。
- **P0-3 / P0-5 会话与确认**：确认队列按 `request_id` 排队消竞态；会话结束观察器真的停；防护状态由状态机从"会话 + 观察器 + 心跳 + 审计可写"推出（active / degraded），不再"会话开着就算守护"；Windows 观察器绑会话并带代际号、失败退避进 degraded。
- **P0-4 真机判据机器化**：壳子在 `AGENTGUARD_ACCEPTANCE_TRACE` 下写 JSONL，`guard-cli acceptance-trace-check` 对照审计库做六项检查；macOS 15–17、Windows W8–W10 用例与证据必填列表三语同步。
- **P1-1 / P1-2 / P1-3 扩展**：Native Messaging 转发默认关闭且设置加载前 fail-closed 排队；外送与本地 URL 最小化；宿主长连接、退避重连、暂停状态持久化；连接级 nonce + 单调 seq 拒重放/乱序。
- **P1-4 后台 Critical**：确认到达拉前窗口、菜单栏 / 标题计数；两分钟未拍板按拒绝写 Timeout 回执；待确认落盘并在重启后逐条写回执。未接系统级通知。
- **P1-6 Android**：守护 / 中继状态由状态机推出；进程重启后失败关闭并要求用户重新开始会话，不自动恢复观察；普通常驻通知只反映当前绑定会话；令牌进 Keystore、不回显；事件 JSONL 14 天 / 20 文件 / 50 MiB / 5 MiB 轮转 + 清除按钮。
- **P1-7 Local API / webhook**：审计库默认私有目录并拒符号链接与共享可写目录；请求体 256 KiB、`limit` 1000；令牌脱敏；webhook 必带 `event_id / created_ms / version`，幂等、±10 分钟、版本单调。
- **P1-8 / P1-9**：CI 签名步骤在替身产物上真跑 ad-hoc 签名并验；设备策略只装验过签的且只收紧不放宽，未验证的只显示。
- **Windows 补充报告**：观察器按 pid 跳过 AgentGuard 自己的窗口；OVL-010 加"未渲染文字须像一段指令"判据；W6 能力强制不可用开关；W7 `install-host.ps1`（未在真机执行）。
- **P2-1 覆盖面**：iMy `ask_user` 以 `user_query` 事件进引擎（USER-QUERY / PRIV-GUESS），最后一个 uncovered surface 关闭；图标语料的重复字形修正，通道度量重算。
- **P2-3 告警风暴**：桌面观察去重（`event_dedup`，同内容 30 s 一次，改一字即放行）并修 UI-REVALIDATE 跨来源误判根因；扩展 finding 去重、增量扫描、1.5 s 节流，真浏览器 E2E 加变异风暴回归 M1–M4。
- **P2-4 无障碍与本地化**：桌面弹层 `alertdialog` + 焦点圈 + Esc + 焦点还原 + live region；Android TalkBack 等系统服务不再算"输入被观察"；繁中资源修正；launcher 标签本地化。
- **P2-5 文档漂移**：新增源码生成的三语 [docs/capability-matrix.md](docs/capability-matrix.md)（各端真正发出的事件、静态测试数、版本字符串），`cargo test` 逐字核对；Android 文案不再说"观察深层链接"（只报界面文字里的深链字样，代码从未发出 `deeplink`）；iOS 页面从"我们交付"改为"今天有什么 / 目标是什么"；发布说明与本文件不再自写声明条数。
- **P2-6 复现与供应链**：`rust-toolchain.toml`、`.nvmrc`、Actions 钉 commit SHA；两个桌面壳子的 lockfile 用 `deny.shells.toml` 单独过 cargo-deny（MPL-2.0 逐 crate 例外待法务确认）。
- **P2-7 商业边界**：企业功能只对厂商 Ed25519 签名的授权解锁（到期必填、7 天离线宽限、签名撤销名单）；HMAC / webhook / 夹具签名的授权显示为演示、不解锁。
- **阶段 D 云端准备**：Chromium 真浏览器 E2E（39 条机器判据）与三语 `acceptance-chrome.md`；Windows W3–W5 确定性固件与渲染契约测试；Android adb 验收脚本；Local API 对每份签名 body 的验签结论可读。

**仍未验证（需你侧）**：Developer ID 签名与公证、Authenticode、Android release key、iOS Apple Distribution，以及 macOS / Windows / Android / iOS / TestFlight / Chrome / Edge 当前候选逐项走完并归档 strict 证据。Chrome 与 Edge 各自要求 B1–B5；Firefox 不属于首个 GA，不能产生发布 PASS。


### Added

- 接入 D 亮色品牌方案：增加共享 Logo 与 App 图标母版；更新 macOS、Windows、Android 与 Chromium 图标（含菜单栏、Adaptive/主题及通知小图标）；并在三语 README、文档门户、符合性说明及各前端页眉展示统一品牌标志。
- 新增 `guard-trust`，以统一的常数时间比较、`InboundOutcome` 词汇和入站面清册测试约束六类入站信任边界；各协议仍保留适合自身的密码学原语和信任锚。
- 新增“用户能力声明 ↔ 证明测试”机器可核对映射（当前条数见源码生成的 [docs/capability-matrix.md](docs/capability-matrix.md)），以及从能力声明、发布门禁和状态数据生成的仪表盘；它们证明声明锚点与测试存在，不替代真机验收。
- `guard-jail` 新增可选 `scope.net` 网络天花板：在 Landlock ABI v4（Linux 内核 6.7+）上只允许明确列出的 TCP connect/bind 端口；未声明时不约束网络，已声明但无法强制时拒绝启动。
- 浏览器扩展新增付款 CTA、陷阱表单及付款形状 fetch/XHR 的有限执行前确认门；新增对已知恶意与越出会话范围主机的 DNR 阻断、持久化/过期语义、名单管理和规则溯源。
- Firefox 仅保留独立源码原型 manifest 与 Native Messaging host 接入骨架，并补 Edge 安装兼容；首个 GA 不打包、提交或验收 Firefox。iOS Safari 已有可编译的有限 Swift 工程/SKU，但 Apple Distribution 签名、真机 Safari Extension、TestFlight 与 Rust 引擎接入仍待完成。
- macOS AX 树观测新增 AXObserver 推送、150ms 去抖、800ms 延迟上限与 3s 兜底轮询；像素捕获仍为采样路径。
- macOS、Windows 与 Chromium 界面完成三语消费者化改造，包括首次引导、人话风险文案、无障碍确认层、键盘焦点、深色模式、通知及词表完整性检查。
- 新增 Chrome/Edge、Windows、macOS、Android 与 iOS 验收清单、可执行手册、浏览器夹具和报告模板；文档与夹具是待执行流程，不表示已经获得真机证据。
- 记录 Windows 候选 `89dadf9` 的历史部分真机验收：Windows 11 build 26200 上桌面测试 5/5、Clippy `-D warnings` 与 Release 构建通过；未签名 EXE 的 SHA-256 为 `47A420C6A5FA88C406C18DD7F8A189B6D21183143A2DA69578FA02C559AB5119`。独立 RDP 交互证据覆盖两轮各超过 30 秒的启动与连续观测、UIA/GDI/OCR 可用状态、真实 `OVL-010` 事后风险确认及拒绝后的第二轮稳定性，且未出现新的 Event 1000；它不证明外部动作被阻止，也不是当前 `first-ga-v1` W1–W6/W8–W11 全量验收。
- 首个 GA 严格门禁现有十二类结构化发布证据模板与校验：证据绑定当前完整提交号、实际命令、退出码、时间、判据输出和产物身份。普通发布文件使用标准 SHA-256；macOS/iOS `.app` 的 tree-v2 绑定整个 bundle 的路径、类型、长度、内容及 Unix `0111` 可执行位掩码；验收 closure-v1 则绑定报告 bytes 与每个唯一逐项引用的路径、长度和内容。五类签名证据把 `signer` 绑定到门禁外部提供的 Apple Team ID 或发布证书 SHA-256，七类验收证据固定为 `null`；Firefox kind 仅保留旧版/未来格式兼容，不进首个 GA strict。路径采用可移植 ASCII 组件且逐项引用不得复用；未填写模板、空产物、缺失文件、自引用、符号链接和摘要不匹配均不能通过。tree-v2 不绑定其他 mode、xattr/ACL，也不替代隔离机上的 quarantine、Gatekeeper 和首次启动验收；验收 closure 仍是不能证明截图来源的未签名自证。

### Security

- 网络天花板一经声明便覆盖 TCP connect 与 bind；空端口表表示全部拒绝，非 Landlock 后端不得静默降级为网络开放。
- 恶意主机 DNR 名单跨 service worker 重启保留，越界主机随会话过期；popup 可查看、解除并追溯到 `INTEL-DOMAIN` 或 `SCOPE-HOST`。
- 严格门禁不再接受仅含关键词的任意文件；签名与验收命令必须采用校验器认可的完整 fail-closed 成功链，任一子命令失败都不能被后续输出掩盖。五类签名证据还须出现工具输出与外部预期的 Team ID/证书 SHA-256，并在运行前后核对同一 clean 候选提交。结构化 JSON 仍是未签名自证，只防误绑定、误操作和部分机械伪造，不防控制工作区的攻击者伪造全部字段。

### Changed

- Chromium 不再笼统描述为“只能事后通知”：页面门和 DNR 对其覆盖的向量提供执行前控制；Native Messaging 判决仍是异步的，不能追溯阻止触发事件，且页面门/DNR 都有明确绕过与 fail-open 边界。Android 仍为事件后提示。
- 桌面观测不再笼统描述为“仅轮询”：macOS AX 树变化已有推送；像素捕获、其他桌面路径和兜底仍包含采样或轮询，因此不是零间隙实时监控。
- Windows CI 在现有工作区测试、适配器构建/Clippy 与桌面测试之后新增真实窗口启动 smoke；候选对应的 GitHub Actions run `33551495621` 全绿。该 smoke 只防止窗口启动立即退出，不能替代 `first-ga-v1` W1–W6/W8–W11 的人工交互证据。

### Fixed

- 修复 Landlock 把目录专属权限附到 `/dev/null` 等单文件规则而导致整份规则集 `EINVAL`、子进程未启动的问题；Linux 集成测试现在从已授权目录启动，并直接验证授权读写与真实越界拒绝，不再因未授权的 `/dev/null` 重定向假绿。
- 修复 Landlock 调用 `prctl(PR_SET_NO_NEW_PRIVS)` 时未显式传入三个必须为零的尾参数而可能收到 `EINVAL` 的问题；现在统一使用完整五参数系统调用，并按 Linux x86_64/aarch64 选择正确的 `prctl` 系统调用号，同时保留现有单文件权限过滤。
- 修复 aarch64 的 mount-namespace 降级路径误用 x86_64 `getuid`/`getgid` 系统调用号的问题；现在按架构选择正确编号，并用真实系统调用回归测试钉住回退身份。
- 修复 Windows 默认主线程栈不足时 `guard-cli` 会在进入子命令前溢出的问题；Windows 入口现在以显式 8 MiB 栈运行同一 CLI 调度。发布门禁参数测试在 Windows 上解析 GitHub Runner 的 `C:\shells\gitbash.exe` 绝对路径，并为普通 Windows 回退到默认 Git 安装路径；测试再由原生 `current_dir` 进入仓库并绑定脚本自己的退出码 2 与拒绝文本，WSL、路径或 CLI 启动失败都不能再冒充安全拒绝。
- 修复 Windows `canonicalize` 产生的 `\\?\` verbatim 盘符/UNC 前缀与普通前缀不等价的问题；真实 `C:\Windows`、`C:\ProgramData` 路径重新命中敏感目标，固定的 `\\?\` 命名空间标记也不再被误判为通配符。
- 保留 Windows 组件级路径归约与现有 home、`ProgramData`、`Program Files (x86)` 敏感路径保护；未采用会把不同路径形状全局折叠并造成保护降级的方案。
- 修复 Windows 工作区测试仍把 `/bin/*`、`/srv`、`/tmp` 和 `/etc` 当作跨平台夹具的问题；网关改用可控 Rust 子进程验证并发管道、UTF-8 截断与退出码，路径、Shell 和 jail 测试使用目标平台真实的绝对路径，同时保留敏感目录与参数注入覆盖。
- 修复 Windows 桌面启动时先在主线程以 MTA 初始化 UI Automation，随后 `OleInitialize` 需要 STA 而触发 `RPC_E_CHANGED_MODE` 并退出的问题；启动能力探测现在使用专用线程并缓存结果，窗口主线程不再被预先改为 MTA。
- 修复短命能力探测线程结束后复用 WinRT OCR `FactoryCache` 可能触发 `0xC0000005` 的问题；进程期 `CoIncrementMTAUsage` cookie 保持 COM MTA 可用，并新增 COM/OCR 跨线程回归测试。
- Firefox MV3 源码原型 manifest 改用其支持的模块化 `background.scripts` 事件页；结构测试钉住该原型与 Chromium service worker 的同一 `background.js` 入口，但首个 GA 不生成 Firefox 包。
- 修复读取拦截名单时丢失规则溯源，以及“允许一次”用 `form.submit()` 绕过表单校验并丢失 submitter 语义的问题；付款按钮的 click→submit 链现在共享一次性批准，不会重复弹出确认。
- 把 macOS AXObserver 真正接入桌面驱动，绑定持续运行的主 RunLoop，并随前台应用切换重绑；新增产品路径接线测试。
- macOS / Windows 发布壳把 SQLCipher 与可用审计签名器合并为失败关闭的启动合同；macOS 两个 secret 存入 Keychain，Windows 存入不同类型的 DPAPI envelope。旧明文 DB/WAL/SHM/key 保持不变并阻断，禁止静默切换同级新库造成历史消失。
- macOS `.app` 现内置规则、任务计划、授权策略、签名威胁情报和公钥；Release 只从签名 bundle 加载。移除无依据的 JIT、无签名可执行内存及禁用 Library Validation entitlement；正式签名固定 Team ID，公证只接受 Keychain profile。
- 扩展打包改为先生成全新 ZIP 再原子替换，避免 `zip` 更新模式因源文件时间戳而保留旧代码。

### Known limitations

- 当前 macOS 本机候选仍需以本轮最新 universal `.app` 重跑完整清单，Developer ID / 公证后的全新安装、升级与 TCC 仍未验收；Chrome/Edge 候选包仍需分别做干净 profile 真机验收，Firefox 不在首个 GA。iOS 已有可编译的有限 SKU 与模拟器证据，但签名真机未验。Windows 仍需当前候选 `first-ga-v1` W1–W6/W8–W11 真机全量证据。
- 页面门能覆盖的只是在已安装扩展可触达的页面向量；DNR 规则安装失败时 fail-open，Native Host 和 Android 通知不能提供不可绕过的执行前控制。
- 当前尚未配置正式 macOS/iOS Apple Team ID、Windows/Android 发布证书 SHA-256，五类签名检查保持 `UNVERIFIED`；桌面 Release 已强制 SQLCipher，但 Windows EXE 仍为 `NotSigned`，且没有正式安装包及全新安装、升级、卸载证据。公证、真机与回滚证据仍不完整；结构化证据门禁上线不改变生产发布 **No-Go** 结论。

## [1.0.0-rc.1] - 2026-08-28

> 源码候选版，不代表生产安装包已具备发布条件。当前没有完成代码签名、公证、商店发布或真实设备端到端验收，生产发布判断仍为 **No-Go**。

### Added

- 跨平台 Rust 规则引擎、OP/TR/FM 隐私评分、会话计划与能力范围判决。
- macOS AXUIElement、ScreenCaptureKit 与 Vision OCR 观测路径。
- Windows UI Automation、GDI 抓帧和 Windows.Media.Ocr 实现。
- Android AccessibilityService 伴生应用、环境调查和 Android Keystore P-256 适配器签名。
- Chromium MV3 扩展、Native Messaging host、高风险判决通知，以及对有限页面向量与名单主机的执行前控制。
- 合作式 MCP 工具网关，以及 Linux 上由内核执行的 `guard-jail` 文件系统边界。
- Ed25519 威胁情报、哈希链审计、可选逐条签名与 SQLCipher。
- Bearer 保护的本地 API、签名策略同步和已认证计费 webhook。
- 离线评测、覆盖矩阵、预检和发布证据门禁。
- 简体中文、繁體中文与英文的核心 README、文档门户、发布说明和变更记录。

### Security

- 发布路径拒绝以 `sha256:` 完整性摘要冒充威胁情报真实性签名。
- Native Messaging 调用者身份默认 fail-closed。
- 敏感文件系统目标改为不可确认放行；网关文件操作进入引擎独立判决，宿主接入审计存储与签名器后才写入可验证审计。
- 修复路径归约、符号链接、macOS 卷别名、root mount namespace 与读取范围问题。
- 加固审计见证包含性、会话计数、密钥文件权限、前端 DOM 写入与 CSP。
- 让策略同步和计费 webhook 在跨越信任边界时验证签名。

### Changed

- 明确区分旁路观测、合作式控制和 Linux 内核执行边界。
- Android 的确认仍是事件后通知；Chromium 的页面门与 DNR 则在其有限覆盖面内提供执行前控制，Native Messaging 判决仍为异步。
- Windows 状态从模拟脚手架更新为真实 UIA/GDI/OCR 实现，同时保留“尚未真机验收”的限制。
- `guard-ffi` 明确标记为仓库内没有消费者的实验组件。
- 发布文档不再把源码、测试、构建和正式安装包证据混为同一状态。

### Known limitations

- 除 Linux `guard-jail` 外，大部分控制依赖 Agent 主动经过 AgentGuard，可以绕过。
- macOS AX 树变化已有推送，但像素捕获、其他桌面观测与兜底仍包含采样或轮询，不是零间隙实时监控。
- Android 无法在动作发生前阻断；Chromium 只能在页面门和 DNR 覆盖的向量上执行前控制，不能据此声称通用或不可绕过。
- Windows 尚无真实设备端到端验收；iOS 只有有限脚手架，没有完整工程或引擎接线。
- 仓库夹具密钥不得用于生产，部署前必须替换。
- 尚无签名、公证安装包和真实设备验收证据，严格发布门禁不能通过。

完整范围与复验要求见 [1.0.0-rc.1 发布说明](docs/RELEASE-1.0.0-rc.1.md)。
