# AGD-027：M3 联合验收

日期：2026-09-16。状态：**开发中，尚未通过整体验收**。真实恢复／撤钥、原工作区 50 项任务／100 次循环／30 分钟持续运行、浏览器及网络故障均通过；首轮性能仅 4/6 达标，尚不能宣布 M3 通过。F13 按用户要求暂缓、未验收，完整原 M1 与发布均保持 No-Go。

## 候选与原要求

本轮不重新构建 App。固定日常入口仍为 `apps/desktop-macos/src-tauri/target/debug/bundle/macos/AgentGuard Local Agent Test.app`，**Ver 2.1（020）**，Bundle ID `com.agentguard.desktop.macos.localagenttest`。名称、路径及组织签名要求不变。

M3 CLI 使用 AGD-026 冻结网关，SHA-256 为 `a250be2538a199d3a01124ba55d01213ce0333e00df92697124392ccc696d761`。开跑前重新核对其 211 项冻结输入与当前文件逐字一致；没有将旧 AGD-021～025 的不同候选直接算作本轮通过。包内托管网关仍为此前稳定版本，M3 记忆／委托必须显式配置外接 CLI，不能把 App 的普通本地任务表单当成已接入这两项能力。

完成标准沿用开发计划：正常、攻击、故障恢复均有实际效果；跨重启状态及密钥撤销分别验证；受影响 M1 的 50 个完整任务、100 次循环、30 分钟运行与故障种类不删减。平台覆盖逐项注明，CI 编译不能替代原生设备验收。

## 已完成的恢复与密钥专项

使用全新合成工作区、合成 Ed25519 密钥和同一既有 Docker 镜像，四个 CLI 进程串行运行。没有改动用户资料或现有授权。

| 场景 | 实际结果 |
| --- | --- |
| 待批准记忆写入时 SIGKILL | 已批准的版本 1 保留；未批准的新正文未写入；重启后完整来源、标签及批准引用保持一致 |
| 重启后旧治理批准 | 旧撤销预览返回 409，没有新增版本 |
| 同时配置记忆与委托 | 操作者仍可核对记忆历史；模型只得到签名委托入口，不能绕回普通 `memory_write` |
| 更换主体公钥 | 旧宿主正常结束后换公钥并重开；新会话、新授权、正确序号的消息使用旧私钥签名仍被拒绝 |
| 新密钥正常性 | 对上面**同一条消息**仅更换为新私钥签名，实际读取成功；排除了旧会话校验提前拒绝的假通过 |
| 移除主体 | 同时移除指向该主体的委托权限；新宿主拒绝其消息及再次委托，父主体仍可读取原合成文件 |

**撤钥的生效条件是先停止旧宿主，再修改宿主配置并重新启动。** 当前配置在启动时加载，没有运行中自动重载主体公钥的能力。界面停止委托分支是另一项控制，不能替代主体密钥撤销；已经完成的外部操作也不会因撤钥而撤回。记忆签名密钥与委托主体密钥分别管理，本项没有实施历史记忆换钥迁移。

独立核对器只读真实 SQLite 和公钥：核对 **49 条执行审计、2 条记忆审计、17 次签名结果、5 条成功消息及 4 条拒绝消息**。替换撤销旧钥签名、隐藏 SIGKILL、删去正常读取、恢复已移除主体四类篡改报告均被拒绝。

初次测试夹具仅声明记忆操作，却调用了隔离文件读取；网关按现有任务范围规则附加说明，使精确正文断言失败。已在夹具中预先声明文件执行步骤，并保持只读目录和委托文件权限限制，随后重跑通过。失败报告 `recovery-01/` 原样保留，没有改产品规则或放宽结果断言。

## 正常任务与当前原生界面

原 T01～T50 **50/50 通过**，工作区循环 **100/100 通过**，二者独立计数。分别保留输入、执行、预览、回写结果及成果摘要；原分母不变。它们是脚本驱动的完整合成任务，不能表述为模型自主任务或 50 次人工操作。

本次通过 Computer Use 重新查看准确 020 App：两项权限仍显示已授权；活动时间线实际显示本地日期、时分秒和时区，并保留 UTC 值。OVL-007 显示为附原因的普通观察记录。用户所示 CRIT-005 普通安装文字修复已包含在同一 App 中，但 Safari 全部正文的原生读取链路仍有验收缺口，不以没有新弹窗证明完整修复。OVL-009 等其他观察告警不属于此项已修复声明。

## 平台与尚未完成项

| 平台／入口 | 本轮证据范围 |
| --- | --- |
| macOS CLI 与 Docker Linux 执行器 | 恢复、撤钥、50 项任务、100 循环、30 分钟运行及 Docker 虚拟网络故障实际通过；性能仍未通过 |
| 准确 macOS App 020 | 当轮权限与时间显示实查；AGD-026 治理和分支停止的既有证据已重新核对，91 个包文件相同；随后仅测试源码发生下述差异 |
| 包内本地模型任务 | 仍使用稳定网关；未自动配置 M3 记忆和委托 |
| 独立 Linux 宿主 | 本轮未实机验收；Docker 执行器不能替代 |
| Windows、iOS、Android | 未验收本轮 M3 原生能力；既有 CI 结果分别列账 |

工作区故障 **50/50** 已通过；记忆／RAG 四进程、3 个模型任务及持久投毒／撤销／传播六进程、2 个新模型任务均通过。委托预算六进程故障专项也已通过，独立核对 113 条审计和 33 次签名。原定义持续运行、浏览器与网络故障复验现已完成；逐项结果见下表。F13 继续独立保留，不写成通过。

## 联合复验与性能缺口

| 原要求 | 本次同一冻结 CLI 的结果 |
| --- | --- |
| 工作区 50 项完整任务 | 50/50；逐项输入、预览、回写结果及成果摘要核对 |
| 工作区 100 次循环 | 100/100；没有用单步请求充当完整任务 |
| 原 30 分钟持续运行 | 1,800,627 ms，30 个任务，121 次同一会话心跳，最大间隔 15,851 ms；结束无自有容器遗留 |
| 工作区故障与隔离 | 50/50 故障、78/78 隔离检查 |
| 浏览器正常任务与循环 | B01～B10 全部通过，100 次循环中 50 次批准对应 50 个实际 POST；40 次拒绝、10 次断连均无 POST |
| 浏览器真实崩溃 | 待批准时 SIGKILL，旧批准 409、零 POST；新会话批准后仅一个 POST，读回结果一致 |
| F14 实际断网恢复 | 同一 Docker 服务进程断开／恢复非 loopback 虚拟链路，25,190 ms 无自动重发；旧批准及旧会话恢复 409，新请求只提交一次 |
| 既有治理证据重验 | CLI 三进程 24 次请求及原生 020 的 12 次签名重新核对；属于留存证据复核，本轮没有重做治理 UI 操作 |

`verify-m3-joint.py` 除报告外，还读取实际子任务文件与 SQLite：**1,225 条浏览器审计、33 条 F14 审计**逐项绑定实际批准／拒绝回执和收件正文。六类报告副本篡改均拒绝：删去循环、缩短运行要求、替换心跳会话、替换批准回执、改已批准正文、复制性能路由冒充另一入口。原始文件不改，核对器不会因功能读取成功就把超预算改为通过。

首轮每路由预热 5 次、正式 30 次。原生读取 p95 **10.8635 ms > 10 ms**，隔离读取 p95 **741.4758 ms > 500 ms**；两条路由的启动时间与网关 RSS 在原预算内，合计 **4/6**。原始 benchmark 的 `state=passed` 仅说明实际读取内容正确，不是六项性能预算通过；联合核对器因此返回 `evidence_verified=true, passed=false` 和非零退出码。

同机保留了大量 Chromium 编译进程，18 核机器首轮 load average 约 23～25；这是同步观察，尚不能证明它是全部原因。预先保存旧、新、新、旧的四轮对照计划，固定样本数与阈值，保留每轮主机负载及全部样本。对照不覆盖首轮失败，不停止其他项目进程，也不通过放宽预算获得通过。

四轮对照已完成，逐条原始样本重算并核对报告摘要：

| 顺序 | 网关 | 原生读取 p95（≤10 ms） | 隔离读取 p95（≤500 ms） | 原预算 |
| --- | --- | --- | --- | --- |
| 1 | 旧 | 10.0091 ms | 592.0678 ms | 4/6 |
| 2 | 新 | 13.4405 ms | 576.8173 ms | 4/6 |
| 3 | 新 | 11.2458 ms | 572.8078 ms | 4/6 |
| 4 | 旧 | 10.8901 ms | 606.5162 ms | 4/6 |

旧、新候选均超时延预算，因此不能将首轮失败单独归因于新增记忆／委托功能；本组样本也不足以证明“完全没有回归”或“全部由机器负载造成”。保持 027 开发中，后续在可比较负载下定位调度／Docker 执行耗时并按原预算复验；不会反复重跑挑选通过样本。

本次 F14 是 Docker 虚拟链路和脚本批准；不代表 Mac 物理网络恢复、人工确认重跑或 F13。包内托管网关与本轮显式 M3 CLI 的证据继续分开。

## 复现与证据

新增入口：

```bash
node scripts/acceptance/agd-m3-recovery.mjs \
  --binary .artifacts/m3-memory-governance-2026-09-16/frozen-03/agentguard-mcp \
  --out .artifacts/m3-joint-2026-09-16/recovery-new
python3 scripts/acceptance/verify-m3-recovery.py \
  .artifacts/m3-joint-2026-09-16/recovery-new \
  --frozen .artifacts/m3-memory-governance-2026-09-16/frozen-03
```

要求原 Docker VM 已运行、使用原冻结镜像和仓库约定的 Node；输出目录必须不存在。原始证据位于 `.artifacts/m3-joint-2026-09-16/`，包含 `recovery-02/`、`recovery-independent-01.json`、`recovery-tamper-results.json`、`workspace-tasks-01/` 及 020 原生时间截图；公开[证据索引](evidence/m3-joint-2026-09-16.json)仅保留汇总与摘要。真实控制凭据和测试密钥不提交 GitHub。此阶段没有产品运行时代码变更，撤销本次验收脚本不影响现有 App 或历史存储。

## CI 首败与修正

提交 `2a23272` 的 [CI 35036886502](https://github.com/gcsagroup/agentguard/actions/runs/35036886502) 为 12/13，通过不能覆盖 macOS 桌面测试失败。已在本机用分段 HTTP 正文复现同一 `WouldBlock`：测试监听器为非阻塞，macOS 接收的连接继承该状态，设置读取超时不会将它改回阻塞。测试服务器因此在正文送齐前关闭连接，协议拒绝测试错收到 `GATEWAY_IO`。

现在只在 `cfg(test)` 夹具中将接收连接设回阻塞，并新增分段中文正文回归。首次回归失败原样保留，修正后完整桌面测试 **134 通过、11 忽略**，仓库不变量 **23 通过**，三语能力矩阵同步。没有延长产品超时或放宽协议校验。

本次源码对 020 的 36 项原生输入中仅测试模块一项有差异，该文件测试模块之前的生产代码逐字相同；91 个已签名 App 文件及严格签名再次核对保持不变。原 `app-020-revalidation.json` 记录的是修改测试前的全输入一致状态，新边界见 `.artifacts/m3-joint-2026-09-16/ci-repair-01.json`。本次没有重新打包或生成新的 App 名称。

修正提交 `84b6a38` 已推送；[CI 35038753030](https://github.com/gcsagroup/agentguard/actions/runs/35038753030) 中原先失败的 macOS 桌面测试步骤已成功。本次记录时 10/13 作业完成且通过，Windows／macOS Release SQLCipher 与 iOS 作业仍运行，尚不写成全部 CI 通过。
