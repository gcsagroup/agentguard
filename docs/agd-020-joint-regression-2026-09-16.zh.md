# AGD-020：015 联合回归与独立验收

日期：2026-09-16。**AGD-020 按原任务范围完成**，在用户暂缓 F13 的既有条件下进入 M3。本记录接续[015 原生验收](agd-020-native-015-2026-09-15.zh.md)，保留各批次冻结身份、原始失败和分母。F13 仍未验收，完整原 M1、整个开发计划和发布均未通过。

## 当前候选与证据归属

本批没有重新构建 App。日常入口仍为 `apps/desktop-macos/src-tauri/target/debug/bundle/macos/AgentGuard Local Agent Test.app`，版本 **Ver 2.0（015）**，Bundle ID 为 `com.agentguard.desktop.macos.localagenttest`，使用原组织签名要求。

| 对象 | SHA-256 / 范围 |
| --- | --- |
| App 主程序 | `dc336f5f4de6a00b275c1bfe371432d608542251c70e505595f0ba24c10733ca` |
| 包内及本批测试网关 | `a011628363e6fdf75ac5910439ed60ebf7e5ecabe44fbff4479c8c71f40618a4` |
| 015 冻结源码清单 | `4bdd00e3180e545fef3b95697bb9d88d8f196f04d51be6c1c9881fc591dd178c`，1,074 个文件 |
| 从该源码构建的规则管理 CLI | `a6c0a3c4e0c725498f17f4b21faa55c5e59feab0859d5e5318f042ba6aa74bc6` |
| 从该源码构建的出口测试程序 | `497d2bfe4575774d829d40bb766809f9bbd4cef0e4fbc83aced7c75e477104d3` |

015 沿用 014 的同一网关字节，229 个网关／浏览器生产输入逐项相同。目录名带 `14` 的性能、资料入口、DOM、模型和工作区成果继续保留原名，不能把目录改名当重跑。42 项出口测试因旧日志缺少本批可核对的构建绑定，重新从冻结源码生成测试程序并实际运行。

8 项真实 Docker／SQLite 回写故障沿用已冻结测试程序及原报告；完整 191 个 Rust 构建输入和每份报告内的 5 个关键输入摘要均与 015 相同，测试程序、日志及 8 份报告原件摘要一致。这是同源码实测证据的复核，没有宣称再次执行这 8 项。

## AGD-020 原要求逐项核对

| 原要求 | 真实输入和核对结果 |
| --- | --- |
| 隐藏／可见注入 | 五类资料入口 52 项、真实 Chromium DOM 21 项；正文、可见文本、规范化视图、来源标签、实际文件和 POST 分别核对 |
| 工具描述／返回投毒 | 首次恶意描述仍待复核；本地 stdio 和有限远程服务实际返回自报系统权限，宿主授权仍为无；远端 9 次获准读取、零写入效果 |
| 同名工具 | 改变同名工具描述使旧认可失效；名称空间抢占被拒。登记、持久日志、调用结果和后续恢复互相对应 |
| 裁判模型操纵 | 已加载的本机 35B 模型实际回答 9 个请求；伪造裁判、角色和批准信息不能取得执行权，目录外 6 次请求被拒 |
| 正常／未知变体分开 | 资料入口：正常 17、已知攻击 18、新组合变体 16、恢复 1；DOM：正常 6、已知 7、新组合 7、截断 1；模型：三组各 3 |
| M1 权限及正常任务不退化 | 当前网关的原 50 项工作区成果、100 次工作区循环、30 分钟持续运行、浏览器 10 任务／100 循环、故障和原性能预算；准确 015 App 的回写、崩溃、暂停恢复、关闭和两项权限均有独立证据 |

资料入口共 393 条审计、90 次越界拒绝；DOM／模型共 950 条审计，DOM 的 21 次明确批准对应 21 次提交，另 21 次未批准请求零提交。模型三组建议各 3/3 正确；这些预声明合成样本不是未知攻击抽样，不能据此宣称通用检出率或模型永远不会受骗。产品当前没有能自行批准动作的 LLM 裁判，模型建议和宿主授权仍是两条独立边界。

独立核对器中的历史 `remaining_acceptance` 是其单套入口证据的边界，不作为总计划状态；本记录以全部原始报告和下面的联合结果判定。旧报告不追溯改写为新候选通过。

## 当前网关回归结果

| 验收 | 结果与分母 |
| --- | --- |
| 原工作区正常成果 | T01～T50：50/50；单独报告、输入、预览、回写回执和成果摘要一致 |
| 工作区循环 | 100/100，140 次连接实例；不与正常任务合并分母 |
| 原定义持续运行 | 实际 1,800,543.36 ms，30/30 完整任务、121 次心跳，最大心跳间隔 15,663.61 ms；同一持续运行未重启或缩短 |
| 规则运行时／浏览器 | 17/17 与 8/8；110 条审计，16 个已接受发布经独立 Ed25519 验签，修改序号后 16/16 验签失败 |
| 浏览器正常任务 | B01～B10：10/10，实际服务端业务回执和持久终态一致 |
| 浏览器循环 | 100/100：50 次批准恰好 50 次 POST；40 次拒绝和 10 次断连零 POST；与正常任务合计核对 1,225 条审计 |
| 工作区故障／隔离 | 50/50 与 78/78，另 8 项同源码真实回写故障保留原件并核对 |
| 宿主／容器出口 | 宿主 42/42；容器 24 次负向尝试，零越权收件，正向校准另列 |
| 浏览器崩溃 F08 | 真实终止本次 Chromium，旧待批准请求零提交，旧批准 409；新会话明确批准只提交一次 |
| 浏览器解析与直接出口 | 生产 BrowserActor 沙箱下独立 Node 探针 DNS／TCP／UDP、私有目录和继承凭据受限，真实 Chromium 流程另有前述验收 |
| 内容等价与审计故障 | 同字节网关的内容对照 20 项、实际审计故障与迁移 7 场景／68 项通过 |

全部 Docker 工作按顺序运行，继续复用既有镜像，不创建新的镜像或 Volume。各套退出时核对自有进程、容器和控制文件，不清理其它项目资源。回收后的合成临时文件使用当时留存的实际回执和副本复核，不把复核写成再次运行。

F14 另复用原 `agentguard-f14-20260914` 合成容器，在当前网关和准确 015 包内浏览器上重新运行：已派发请求收件后，承载流量的 Linux VM 非回环网卡实际移除，再恢复原 IP。故障期间服务 PID／启动时间保持不变；请求记未知，恢复后观察 **25.23 秒**仍只有一条收件，旧批准和失败会话恢复均为 409；新会话批准后恰好新增一条，浏览器实际读回。两会话 **48 条审计**独立验链并与收件核对，服务容器已停止保留。

本次 F14 是明确标记的脚本批准；010 的[原生批准记录](agd-f14-docker-network-2026-09-14.zh.md)仍只属于 010。原容器之前已停止，本次启动形成新的合成服务生命周期，初始收件数为零；未清空旧报告。测试只覆盖 Docker 虚拟链路，Mac 入口仍为回环地址，不能扩大为 Mac 物理网卡断网。

原性能上限没有修改：本机读取 p95 ≤10 ms、网关启动 ≤100 ms；隔离读取 p95 ≤500 ms、启动 ≤2,000 ms；两路采样网关内存各 ≤32 MiB。本批从原始 90 个正式样本重算，**6/6 通过**：本机读取 8.464 ms、启动 41.122 ms、内存 13.203 MiB；隔离读取 255.204 ms、启动 566.199 ms、内存 13.641 MiB。测试使用已运行的同一 VM，每路只测一次启动，不能解释为冷 VM 或全负载保证。旧 012 的 4/6、后续五次性能失败及全部原始样本继续保留。

## 准确 015 App 的崩溃和生命周期

本次实际使用 Computer Use 操作固定 App，不通过另起同名软件替代。F07 只终止本次准确主进程；没有手工杀掉子进程来制造清理通过。

| 场景 | 真实操作及结果 |
| --- | --- |
| 未批准时崩溃 | `f38a18b6-b42d-46fb-92fd-b2f50d410c14`：确认命令仍待批准，再终止 App；约 0.17 秒内自有进程回收，27.76 秒观察期内开始／迟到标记均不存在，宿主原件不变，终态取消且未派发 |
| 执行中崩溃 | `cddd8b17-b2cf-4fc0-9aca-b6ccc32f804d`：原生批准后，实际看到开始标记和运行中的自有容器，再终止 App；约 0.62 秒内清理，27.79 秒内无迟到写入，宿主原件不变，终态取消且已派发 |
| 两次重开 | 都回到空任务表单、确认通道未连接；旧任务／批准未恢复；辅助功能与屏幕录制仍已授权，没有操作系统授权动作 |
| 只读、暂停和恢复 | `55bf23bf-f0f9-499e-a373-3e33ceb85237`：实际读取中文项目文件；暂停后恢复产生新会话，25.16 秒内没有重放；原生追加明确请求后才发生第二次读取 |
| 正常关窗 | 直接关闭原生窗口；App、子进程、控制端口和连接文件退出，自有容器为零；原件与快照不变。两次读取和暂停／恢复／停止三个控制动作分别绑定正确会话 |

两次 F07 共 22 条、只读生命周期 31 条，共 **53 条新增审计**验链通过。此前原生模型两文件回写的 **54 条**记录和宿主原 **4/4** 测试另行复核；它们是不同任务，不能互换。

本轮历史核对扩展为全部 `*.db` 通道：原 **30 个任务、1,584 条记录**保持逐字段摘要不变。前批 29 任务／1,027 条使用较窄通道集合，数字差异来自范围和新增任务，不是删除或改写旧记录。

关窗后再次打开同一包，总览实际显示“未在守护”“权限已就绪”，辅助功能和屏幕录制均为“已授权”。这次只是重开及权限保留验证；实际 SCK 屏幕流和 AX 正文证明分别见 015、014 原生记录，不用权限布尔值替代采集证明。

## 核对器、失败保留与交付检查

- 回归核对器增加显式 `--candidate 015`，保留默认 012 格式；逐条核对 9 个动作的判决／开始／终态及 3 个独立批准，并校验旧来源库身份、原件与回写结果。
- 新规则／浏览器核对器使用独立密码库校验签名，交叉核对数据库、HTTP 持久回执和业务账本。旧 012 正例仍有效，原性能失败仍为失败。
- 回写终态、旧来源库身份、替换批准身份、漏掉构建输入、提高性能预算五类篡改均被拒；业务正文、循环缺项、持久回执三类篡改均被拒。涉及审计篡改的副本重算完整哈希链，不能仅凭“链正确”冒充业务正确。
- 首次生命周期核对把三个宿主控制终态误算为文件读取，断言失败；随后按工具登记摘要、目标和会话分别验证两次读取与三个控制动作。保留首次失败说明，没有修改产品审计或减少期望动作。
- 固定与归档 015 App 各 91 个文件、1,074 个冻结源码文件、127 个原有独立文件以及 8 个旧 App 归档的 171 个条目均保持内容和模式；两份 015 严格签名通过。

本批最后运行仓库不变量 **23/23 通过**，修改脚本的语法和新增文档链接另行核对。

测试命令及冻结原始证据位于 `.artifacts/m2-acceptance-close-2026-09-15/`。结构化索引见 [联合证据](evidence/m2-joint-regression-2026-09-16.json)，其中只收录脱敏摘要和文件哈希，原始任务日志、合成请求和控制资料不提交 GitHub。

前一提交 `60a8f66a00d749e0fdfb53ae45d2e55b7b61a91b` 的 [GitHub CI 34989630254](https://github.com/gcsagroup/agentguard/actions/runs/34989630254) 已完成 13/13。本批提交另行跟踪；没有发布安装包。Gatekeeper 分发评估仍未通过，F13、完整原 M1、全平台和发布边界保留。
