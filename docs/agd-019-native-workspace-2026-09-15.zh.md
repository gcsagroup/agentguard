# AGD-019：知识库与执行记录的原生验收

日期：2026-09-15。结论：AGD-019 按原计划的工作区功能范围完成；准确 Ver 2.0（012）已实际验证知识库、阶段资料关联、四类执行记录、损坏通道与重启读取。F13 继续暂缓且未验收，完整原 M1 和整个开发计划未完成，发布仍为 No-Go。

本记录接续[知识库实现](agd-019-knowledge-workspace-2026-09-15.zh.md)与[受控执行证据实现](agd-019-execution-evidence-2026-09-15.zh.md)。原计划要求手法→案例→规则→平台覆盖→证据，以及告警／阻断／未知分类；本轮补齐该范围的原生操作。自动化无障碍树有漏报，单独保留为兼容性待查项，不宣称原生读屏验收通过。

## 候选与修改

- 固定路径：`apps/desktop-macos/src-tauri/target/debug/bundle/macos/AgentGuard Local Agent Test.app`。仍使用 `com.agentguard.desktop.macos.localagenttest` 与 `local-agent-20260910` 数据环境。
- 新增受版本管理的 `tauri.local-agent.conf.json`，固定本地验收名称、窗口与版本；基础发布版本仍为 `1.0.0-rc.1`。多项知识与记录功能合并后使用 Ver 2.0，每次实际构建递增：011 → 012，最终 `2.0.0-dev.12`、构建号 `12`，三语版本显示一致。
- 受控记录区域增加名称与程序焦点；切换来源后键盘可继续进入任务与结果选择。Chromium 中验证区域名称和 DOM 焦点；该断言不等于 macOS 读屏通过。
- 每个候选独立冻结 1042 个源码文件，保留源码归档、构建命令、日志和签名 App。010 原包保留归档；同一路径已更新为 012。

| 身份 | SHA-256 |
| --- | --- |
| 011 App 可执行文件 | `4b7b5000f1837e626c39d70907b0746ffa0797d5da3afa57b5a71e6ed0f7bd65` |
| 012 App 可执行文件 | `648fb6147c3d0a20fb08b26e205b5cd3fa7263404080b8a6046e87db71c8bbda` |
| 011／012 包内网关 | `948d5d25109a21b93e25317054868d74c572af310c985c0ce546bd2c51dee97d` |
| 固定知识目录 | `7c3c1afccdb65cba984d86f92cfe6176fc4fe07355d7604b73c54fd127fcbe9b` |

现有组织签名和严格整包校验通过。Gatekeeper 返回 3，本地组织签名不代表 Developer ID 公证或可对外分发；没有创建 Release 或发布安装包。

## 原生实际结果

通过 Computer Use 操作固定路径 App，图像与工具返回文字保存在 `.artifacts/m2-native-workspace-2026-09-15/native-cua/`。本轮读取既有任务，没有把历史任务重记为新执行，也没有启动新的模型任务。

| 操作 → 核对 | 实际结果 |
| --- | --- |
| 打开知识库并切换手法 → 核对目录与阶段 | 18 个手法、7 个阶段；所有阶段实际发生均未知。 |
| 展开资料 → 核对来源、规则、覆盖及证据 | 案例原始 HTTPS 地址作为文本展示，规则 `OVL-004`、登记覆盖未知；`AS-001` 未运行且没有实际执行证据。全目录 12 个场景保持未运行。 |
| 搜索规则编号 → 核对关联结果 | 输入 `OVL-004`，准确显示 2/18；没有自动访问来源或执行资料正文。 |
| 切换受控来源与历史任务 → 核对真实日志 | 选中任务网关 54 条对应 19 个动作，浏览器 6 条对应 2 个动作；共 60 条记录、21 个动作经独立脚本逐项核对。 |
| 筛选阻断并展开阶段 → 核对未派发拒绝 | 1 个阻断，序号 19 为拒绝判决、20 为拒绝终态，`dispatched=false`；规则为 `SHELL-METACHAR`。 |
| 筛选告警 → 核对缺少终态 | 1 个待确认判决，显示“已有判决，尚无执行终态”，没有升级为已阻断。 |
| 筛选未知并展开浏览器阶段 → 核对已派发结果 | 网关 4 个已派发失败保留未知；浏览器 2 个未知。展开浏览器动作的序号 1／2／6，分别对应判决、开始、未知终态。 |
| 筛选普通记录 → 核对工具与业务边界 | 13 个普通记录，显示“工具已返回，业务效果仍需核对”。 |
| 读取本轮独立损坏副本 → 核对通道隔离 | 只修改私有 UUID 副本中的一条记录而不重算链；网关显示不可读且旧卡片清空，浏览器仍显示 6 条有效记录、2 个未知动作。原始目录不修改。 |
| 真正退出再从同一路径启动 → 核对持久读取 | 旧进程 27476 已退出，新进程 28313；重新打开知识库、搜索及损坏副本，结果保持一致。不是刷新页面代替重启。 |
| 将自建副本移回证据目录后刷新 → 核对恢复 | 负例不再出现在列表，原始任务恢复为 23 条／8 个动作，旧错误清除；保留完整负例以便复核。 |

最终对全部 **28 个原始任务、993 条网关／浏览器审计**逐条验链，比较前后规范化内容摘要、行数、链头与任务集合，全部一致。只读 SQLite 可能产生辅助文件、影响目录修改时间；页面明确按目录修改时间排序，不能将其解释为新的动作时间。模型出口日志不在此记录页及本轮前后快照范围内。

## 自动检查与保留问题

本轮冻结候选桌面库 128 项通过、11 项忽略；记录页 18 项、原工作区 124 项（包含三语完整性）、仓库约束 23 项及真实宿主读取导出 1 项通过。根工作区格式检查通过。上一实现提交 `f3301fe73799421c19c3ae2955794befa92075ca` 的 [GitHub CI](https://github.com/gcsagroup/agentguard/actions/runs/34944610415) 为 13/13 通过。

前批全仓 1433 项、31 项新鲜执行和 84 条审计证明仍属前批候选；本轮没有把这些数量标成 012 新执行结果。012 未重跑完整 M1、休眠、性能或长期运行矩阵。

保留以下失败与限制：

1. 011 发现自动化无障碍树漏报受控区域。012 加入区域名称与焦点后，鼠标、键盘、筛选和展开功能验证通过，但工具树仍漏报部分可见控件；重新取句柄与刷新工具会话未消除。根因尚未确认，原始文字与截图保留，不归因于特定 WebKit 缺陷。
2. 原生粘贴曾返回工具超时；重新读取显示文本已经进入且搜索为 2/18，没有盲目重复输入。下拉菜单操作中间态未计为通过。
3. 宿主导出首轮使用错误测试名，实际运行 0 项；修正精确名称后明确运行 1 项并通过。独立核对首轮错误地把模型出口数据库算入仅有两通道的快照集合，断言失败；修正范围后 993 条原始审计全部通过，失败日志保留。
4. 哈希链仅证明当前快照内部一致，未验证签名归属，也不能阻止管理员整体替换日志。知识规则编号是参考关系，不证明实际策略版本一致，更不证明攻击阶段已经发生。

## 证据与回退

结构化索引见 [原生证据](evidence/m2-native-workspace-2026-09-15.json)。本地完整证据根为 `.artifacts/m2-native-workspace-2026-09-15/`：`candidate-11/`、`candidate-12/`、`native-cua/index.json`、前后审计快照、`negative-fixture-preserved/`、`independent-native-verification.json` 与各测试日志。构建脚本 `build-candidate-12.py` 保存准确冻结、固定输出路径、包内网关和组织签名流程；后续构建须递增版本，不能直接重复使用 012 身份。

最终核对独立保留文件 127 个全部一致；010 归档 90 个文件、011 归档 91 个文件、012 固定路径与归档各 91 个文件均与各自构建清单相符，严格签名验证通过。原始未提交资料保持独立，未加入本次提交。

代码回退可撤销本批提交；恢复本地 App 前先退出空闲候选，再使用已核对的完整旧候选归档恢复到同一固定路径并重新验证签名，不能只替换可执行文件。回退界面不更改知识目录、原始审计与安全策略。本轮自建负例已经移出应用目录，不需清理原始任务。

接续 AGD-020：按固定候选进行独立 M2 验收，预先登记正常、已知攻击、未知变体与故障的分母，保持现有 M1 门槛及 F13 暂缓例外；不能把知识库场景的“准备好”改写为产品已通过。
