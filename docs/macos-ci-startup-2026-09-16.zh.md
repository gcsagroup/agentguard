# macOS 合成网关启动超时：诊断证据

日期：2026-09-16。源码基线：`8b3418b44508ba26d81f2bc37035b00776593230`。本批只修改测试夹具；固定 App 保持 Ver 2.1（023），没有重新构建。

## 当前结论

此前 `b598008`、`e56ce1e` 的 CI 均在“停止期间迟到成功回执保留完成事实但不复活任务”中，等待首个 `/v1/chat/completions` 超过原 8 秒期限。两次快照分别为 `starting` 和 `running`，后续合成场景通过。该作业已经串行执行，不能继续把测试间并发当作已确认根因。

基线 `8b3418b` 的 [CI 35081607305](https://github.com/gcsagroup/agentguard/actions/runs/35081607305) 中，桌面测试步骤这次成功，09:52:45 UTC 完成；随后整个 macOS 作业成功。采集时 12 个作业成功，Windows 尚未结束。这证明存在间歇性表现，不证明原因已经消除，也不改写前两次失败。

## 改动和验证

- 合成模型记录连接接收、完整请求和回复成功／失败的相对耗时；合成 Python 网关记录解释器入口、标准库导入完成、监听绑定、RPC 方法及控制请求。两侧提供 Unix 毫秒时间锚点，便于对齐启动过程。
- 只有测试超时时输出阶段信息，网关日志读取上限为 8 KiB；不输出模型正文、令牌或完整进程环境。产品逻辑、8 秒期限、原断言和忽略项保持。
- 本机完整桌面测试 **136 通过、11 忽略、0 失败**；Clippy 全目标 `-D warnings`、格式和仓库 23 项检查通过。此前聚焦运行的 21 项通过不重复累加。固定 App 023 的 91 个文件与原清单逐项相同。
- 在新建测试子进程中，分别于 Python 入口前、入口后暂停其直接创建的合成网关。两次均触发原超时，测试退出码为 **101**；诊断分别显示“网关尚无入口记录”和“已经进入 Python”。这是诊断有效性的故障注入，不能算作托管机器根因复现或功能测试通过。
- 故障注入器首次在恢复已被测试正常回收的子进程时遇到 `ProcessLookupError`；原失败日志保留，处理正常回收竞态后两种输入均完成验证。只操作本脚本创建的测试及其直接子进程。
- 本机以与网关相同的最小环境单次运行 Python 标准库导入，系统入口约 99 ms，Command Line Tools 解释器约 37 ms。不能据此解释托管机器的 8 秒超时，未擅自更换 CI 解释器或添加预热。

原始证据位于 `.artifacts/macos-ci-startup-2026-09-16/`。后续若 CI 再失败，以新时间线判断阻塞处，再做对应修正；当前仍记为根因未确定。AGD-027 性能、F13 暂缓、28／1／1／2 任务计数和发布 No-Go 保持。
