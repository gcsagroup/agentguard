# AGD-032 前置复验：17 项自动门禁通过

日期：2026-09-16。准确候选：`de8fe001bb98d8689bfb842cf142416f6a06c834`。**完整 GA 门禁的 17 项自动检查通过、0 项失败；19 类正式证据未配置，退出 1，发布仍为 No-Go。**

## 实际运行

在另一个干净工作树执行原有 `scripts/release-gate.sh --ga`，开始及结束均核对完整 HEAD 和干净状态。固定 Rust 启动器、Node 22.23.2、现有 `cargo-deny 0.20.2` 与 Apple 工具链位置明确；复用既有编译目录。完整运行约 596 秒，未修改脚本、阈值、默认策略或证据要求。

通过范围包括候选前后冻结、固定工具链、格式、Clippy、三套依赖的供应链检查、全仓测试、离线评测、覆盖与能力映射、前端和脚本、浏览器执行前阻断、macOS 编译与路径语义、部署基线、实际最低版本检查及生产自检。逐项名称和结果见[结构化证据](evidence/m3-release-gate-recheck-2026-09-16.json)。

本次确实重跑了修正后的完整门禁，区别于[前批首败及分项复验](agd-032-release-gate-2026-09-16.zh.md)。前批 `5c771f9` 的 15 通过／2 失败、工具路径问题和非阻塞连接测试首败全部保留，未用新结果覆盖旧报告。

同一提交的 [GitHub CI 35090234556](https://github.com/gcsagroup/agentguard/actions/runs/35090234556) 已完成 **13／13**。它与本机完整门禁分别记录；CI 不替代真机、正式包签名或商店验收。

## 仍未通过的条件

本次未提供 12 类 RC 及 7 类 GA 材料。其范围仍包括六端签名与公证、正式候选真机／浏览器／TestFlight 验收，以及 SBOM、隐私声明、14 天 Beta、两个 RC、五方签字、六渠道安装冒烟和分阶段扩量。门禁按原规则如实判为 No-Go，没有使用开发 App 的限定验收代替它们。

AGD-027 性能缺口和 F13 暂缓保持；AGD-032 仍属前置准备，整个计划未完成。固定 App 保持 Ver 2.1（023），没有重新构建、重签名、改名或重绑权限。本批没有进行发布操作。

原始计划、日志、结束状态、独立核对及 CI 回执位于 `.artifacts/m3-release-gate-2026-09-16/after-fix/`。

## 024 源码的完整门禁首轮与报告同步

后续准确提交 `a55a02484906239143bb191a0ac3f6cd326c5091` 已通过 [GitHub CI 35116121424](https://github.com/gcsagroup/agentguard/actions/runs/35116121424) 的 13／13 作业。另在干净工作树完整执行原 GA 门禁，耗时 508.846 秒，结果为 **16 项自动通过、1 项失败、19 类正式证据未配置，退出 1**。不能将前批 `de8fe00` 的 17／17 移作本批结果。

唯一自动失败是收尾的候选冻结检查：Android Relay v2 先前已同步 YAML 声明及文档，但漏掉 `eval/capability-claims.json`、`eval/capability-claims.md` 两份生成报告。门禁重生成报告后，工作树出现差异；声明核对本身通过，不等于冻结候选通过。已按当前源文件重新生成两份报告：36 条声明保持，去重证明测试从 94 变为 95，补入真实签名 HTTP 响应验证及开发验收边界。未调整检查规则或放过文件漂移。

首轮日志、两份实际差异、开始／结束状态及核对摘要保留在 `.artifacts/m3-release-gate-024-2026-09-16/`，见[本批证据](evidence/m3-release-gate-024-2026-09-16.json)。报告同步后的完整门禁需单独记录；此处仍为首轮失败，不宣称已完成修正后的 17／17。

同批还保留[桌面测试启动采样](macos-ci-startup-2026-09-16.zh.md)：本地两轮分别 19 通过／2 失败和 21 通过，调用栈提供脚本入口前等待线索，未确认根因。固定 App 024 的 91 个文件及模式保持，未重新打包、签名或申请权限；F13 暂缓、AGD-027 性能及发布 No-Go 不变。

## 2026-09-17 修正后的完整复验

准确提交 `0068e9450e5a3869d1e0a74d785fe8a889d57673` 在独立干净工作树重新执行完整 `--ga`，于北京时间 00:07:32 结束，耗时 690.633 秒。**17 项自动检查通过、0 项失败，开始及结束的完整 HEAD 和干净状态一致。** 两份生成报告不再漂移；Rust 1.95 全仓、实际 Rust 1.87 全仓、供应链、离线评测、声明核对、前端和浏览器阻断、macOS 路径与生产自检均由本次完整运行覆盖。

19 类正式证据仍未配置，命令按原门槛退出 1，发布仍为 No-Go。首轮 16／1 的失败不改写；本次修正只涉及报告和记录，与 `a55a024` 的运行代码、构建及门禁脚本完全相同。`a55a024` 的 CI 13／13 与本次本地完整门禁分别记录，不将它冒充 `0068e94` 的 CI。

原始日志、源码对照、结束状态及只读核对器位于 `.artifacts/m3-release-gate-024-2026-09-16/after-fix/`，见[复验证据](evidence/m3-release-gate-024-recheck-2026-09-16.json)。本批没有重建 App；桌面测试间歇性启动、AGD-027 原性能预算、F13 暂缓及各端正式发布验收继续保留。
