# 固定 App 023：数据库依赖升级与原生读写复验

日期：2026-09-16。源码基线：`e56ce1ee91b0b513a7e3251132d5aa0b7c4dfbc0`，本批仅递增桌面构建元数据和三语页脚。固定 App 已原路径更新为 Ver 2.1（023）；名称、Bundle ID、工作区和组织签名保持。

## 本批交付

将[数据库 WAL 修复](agd-027-sqlite-wal-2026-09-16.zh.md)实际带入桌面程序及包内 `agentguard-mcp`。两者均以锁定的依赖构建，包含内置 SQLite 3.53.2；默认 `audit-sqlite`、原审计文件及签名格式保持。没有切换 SQLCipher、迁移用户数据库或调整同步要求。

旧 022 的 91 个文件及模式已逐项核对后保存为可恢复 tar；原 29 个 setup-assets 另行归档。重新生成安装资源后，唯一变化是网关二进制；其余 28 个文件逐字保持。最终 023 包仍有 91 个文件，29 个安装资源与本批输入一致；扩展生成目录 `dist`、`_metadata` 已移入证据目录，再以同一证书完成签名，严格深层校验通过。未新增日常 App 名称。

## 真实操作与验证

| 项目 | 实际结果 |
| --- | --- |
| 更新前 | 022 未在守护；辅助功能、屏幕录制均已授权；活动时间可见。 |
| 更新后 | 023 原生页显示两项已授权、活动记录已就绪，无需重新绑定权限。 |
| 观察运行 | 点击开始后显示“守护中”，窗口内容与屏幕画面同时观察；trace 有 1 次带事件的 AX 读取、24 次录屏心跳。 |
| 正常安装说明 | 在 TextEdit 打开本批合成资料，安装正文 778 为 `CRIT-005 / Info / LogOnly`，输入框 779 为 `UI-FIELD-OBSERVED / Info / LogOnly`；没有确认入队。 |
| 时间线 | 原生界面显示 `2026/09/16 GMT+8 17:42:05`，解释“安装相关文字不证明实际发起安装”；布局及文字由 Computer Use 状态和截图核对。 |
| 历史保留 | 更新前 774 条审计、13 条会话、13 条回执及原元数据逐字段保留；新会话为 775–780，共 6 条。 |
| 验链与验签 | 全库 780 条审计、13 条回执均通过，13 个历史选择与签名回执对应。公钥取自原库，且与更新前相同；不宣称离机密钥见证。 |
| 同包重启 | 新旧记录、会话和元数据逐字段相同，无自动重放；023 和两项授权保持，最后未在守护。 |
| 包内网关 | 直接运行准确包内二进制，正常读取及判决／开始／来源／结束写入故障五场景、51 项检查通过，包含真实 SQLite 写入失败、独立验链与进程重启。该独立夹具未配置签名，不冒充原生签名审计。 |
| 必要检查 | 仓库不变量 23 项通过；三语页脚 JavaScript 语法检查通过。前批全仓 1,535、桌面 136 和 SQLCipher 120 项保留原执行身份，不重新累加。 |

独立核对器还拒绝旧审计改写、普通安装变高风险、缺少屏幕心跳及重启记录丢失四类篡改。所有原生数据来自准确 023 的真实启停和数据库副本，不以合成 UI 桩代替。临时安装说明窗口已关闭，用户原有窗口保持；验收 trace 只注入第一次启动，同包普通重启后不再注入。

## 保留的失败与范围

- 准备命令首次误用工作目录，备份目标与日志路径不存在，未启动 App 构建；修正绝对路径与工作目录后完成。证书提取参数首次返回错误，改用正确的等号形式后核对证书摘要通过。原错误摘要留档。
- 源码基线 `e56ce1e` 的 GitHub CI 运行 `35080234854` 再次出现原 macOS 合成模型超时：135 通过、1 失败、11 忽略。仍为“停止期间迟到成功回执”测试，等待 `/v1/chat/completions` 超过 8 秒；本次错误快照已到 `running`，前批为 `starting`。这是新的定位证据，不能因此判定数据库升级导致失败或原问题已经解决；未放宽超时、删除测试或重跑掩盖失败。
- 本批验证同路径升级、普通观察、持久记录、重启及包内网关数据库故障。不替代新网关的完整 AGD-027 性能、全部模型任务／浏览器／记忆／委托复验，也不把 022 的窗口卡顿证据改写为 023 重验。
- F13 继续暂缓、未验收；AGD-031 的外部共享需求条件和 AGD-032 的正式发布门槛保持。任务计数仍为 28 完成、1 附条件完成、1 开发中、2 待开始。

原始证据：`.artifacts/native-sqlite-023-2026-09-16/`。公开摘要见[证据索引](evidence/native-sqlite-023-2026-09-16.json)。当前本机签名用于固定开发验收，未公证，不能外推正式发行。
