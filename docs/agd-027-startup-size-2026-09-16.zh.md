# AGD-027：启动体积对照与审计核对补正

日期：2026-09-16。源码基线：`961226a1b7ef50a5b6bb4548e678eba4a5d63fa8`。**移除局部和调试符号不足以解决首次启动超时，本轮不作为产品修复或性能通过。** AGD-027 继续开发中，F13 暂缓、发布 No-Go 保持。

## 对照范围

从此前冻结的 SQLite 3.53.2 开发 CLI（SHA-256 `b5fdf2cab601610bcd7573b3719b868f0fa5f6a7d5915e8649949dae2a9193e0`）制作两个诊断副本。依据本机 `strip` 手册，仅对一个副本执行 `strip -x -S`；两份均使用固定组织证书 `7C33B517942161311A087CE893D2F1CE9B204B44` 签名。14 个实际存于文件的 Mach-O 节逐字一致，两份严格签名校验通过。

同组织签名后，原体积为 18,738,288 字节，精简体积为 15,057,760 字节，减少 19.64%。两份摘要分别为 `7d625a78bd202b31f860813184187779cbaf08ef8f60bf8108887d99a8774aef` 和 `2cecd3f98fe3138e48b5b5160a876b5ea63c7d82b2678c61527e16d14de038a7`。这两个副本不是已安装 App 的网关；未改变正式构建选项。

预先冻结顺序为原体积首次、精简首次、精简重复、原体积重复。保留动态加载诊断日志，使用宿主收到日志的时间；日志开销及单组对照不适合量化稳定收益，也不替代原四轮性能验收。

## 实测结果

| 诊断副本 | 首条加载日志 | 首条应用日志 | 完整握手 |
| --- | ---: | ---: | ---: |
| 原体积首次 | 727.926 ms | 733.230 ms | 789.719 ms |
| 精简首次 | 553.676 ms | 559.149 ms | 587.520 ms |
| 精简重复 | 3.081 ms | 7.981 ms | 34.389 ms |
| 原体积重复 | 3.642 ms | 8.727 ms | 35.041 ms |

两个首次均超过原 100 ms 启动预算；主要等待仍发生在首条应用日志之前。本轮未精确测出某个系统检查各占多少时间，没有通过移除首次样本、修改持久同步或系统安全策略取得通过。四次均实际读取合成 1 KiB 文件并正常退出，返回摘要与文件一致。

## 核对器修正与历史回查

本轮首次核对使用不存在的 `signature` 字段，出现 `KeyError`；错误摘要保留。修正为检查真实 SQLite 字段 `record_sig`、`signer_key_id` 均为空字符串后，32 条无签名审计哈希链、四份读取成果、候选摘要、运行顺序及时间点均通过。七类反例被拒，包括把带签名记录冒称无签名，以及修改真实审计副本；原数据库不变。核对器重复执行结果一致。

同时发现此前启动阶段诊断的 `.get("signature") is None` 并未检查真实签名列。已只读回查原九轮的 27 个数据库：72 条记录的真实签名字段均为空，哈希链正确，原结论仍成立。旧脚本、旧报告和原数据库保留；新增的回查结果与缺陷记录单列，不把原核对器写成从未出错。

运行后核对原 254 项输入，以及固定 App 023 的 91 个文件和模式，均保持。当前源码基线的 [CI 35095880602](https://github.com/gcsagroup/agentguard/actions/runs/35095880602) 已完成 13／13。没有重建 App、调整授权或新增日常 App 名称；当前文件核对不替代新的原生验收。

原始计划、脚本、日志、数据库、错误摘要及反例位于 `.artifacts/m3-startup-size-2026-09-16/`，公开摘要见[证据索引](evidence/m3-startup-size-2026-09-16.json)。[原性能四轮](agd-027-sqlite-performance-2026-09-16.zh.md)与[前批启动诊断](agd-027-startup-phases-2026-09-16.zh.md)继续保留；AGD-027 及正式签名、公证和发布要求没有变更。
