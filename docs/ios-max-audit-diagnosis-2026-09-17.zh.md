# iOS 最大字号：独立对比度诊断

日期：2026-09-17。基线为 `b458ca5b618ae01d22c931c72cb4abac01a55bcc`。本次缩小了排查范围，没有修复或豁免对比度报告，不计 I6 通过。正式源码和 App／Extension 仍为 1.0.0 (22)。

## 实验与结果

此前[构建 21 的最大字号全类别审计](ios-page-audit-2026-09-17.zh.md)存在对比度失败，部分问题元素在审计后的坐标已远离屏幕。为检验是否由其他审计类别改变字号和滚动位置造成，本次在隔离副本中仅运行 `.contrast`，另保存每页审计后的截图和原生树。产品界面源码保持一致，副本构建号递增为 23；三语、最大辅助字号、浅色、逐页重新进入、12 页上限和禁用清除按钮例外均保持。

既有 Aegis QA iPhone 17／iOS 26.5（23F77）上的实际结果为 **1 通过、2 失败、0 跳过**，退出码 65。英文保存 11 页并通过；简中 7 页、繁中 8 页均到达末尾，但对比度仍失败。4 条禁用清除按钮报告符合原来的严格例外，其他 3 条未豁免：

| 语言／页码（从 0 开始） | 元素 | 审计前后相同的纵向位置 |
| --- | --- | --- |
| 简中／1 | 禁用的“启用 Safari 门控”标签 | y=50.0，高 127.3 |
| 繁中／5 | 启用状态说明 `enable.note` | y=-145.7，高 322.7 |
| 繁中／7 | 隐私说明 `privacy.note` | y=-88.7，高 433.0 |

窗口为 402×874，导航栏从 y=62 到 116。三处文字均跨过导航栏附近的裁剪边界；逐张核对了这三处审计前后共 6 张截图，可见部分清晰，位置没有改变。独立 `.contrast` 仍报告失败，因此不能用“全类别审计改变位置”解释全部失败。

裁剪或遮挡是否影响系统对比度判断仍是待验证因素；没有把目视清晰当成失败无效，也没有改变产品颜色、缩小字号、扩大例外或替换正式 `.all` 测试。下一步应在相同环境中比较同一段文字部分显示与完整显示的结果，并保留其他报告。

## 证据与环境恢复

测试于本地 05:33:08 已结束；随后 Xcode 的附属 `simctl diagnose` 等待超过四分钟。保存测试标准输出和 Xcode 采样后，只向该附属收集进程发送 SIGINT；Xcode 以失败码 65 结束，xcresult、附件和失败摘要均正常导出。采样显示父进程正在等待模拟器诊断收集。完整系统诊断未完成，不将其写为成功。

同一模拟器恢复已有构建 22，两项 App／Extension 的可执行文件与此前正式开发产物逐字节一致；读回字号 `large`、外观 `light`，再恢复为 `Shutdown`。没有清空设备或其他 App 数据。固定 macOS App 024 的 91 个文件及模式也保持，桌面观察专项重新执行 12／12 通过；首次命令使用不存在的 Cargo 路径，未开始测试，改用仓库固定工具链脚本后通过。

原始副本、xcresult、前后截图、全部附件哈希与恢复记录位于 `.artifacts/ios-max-audit-diagnosis-2026-09-17/`；[公开证据索引](evidence/ios-max-audit-diagnosis-2026-09-17.json)保留结论和哈希。主工作区没有替换 UI 测试或产品代码。

同一基线的 GitHub CI 已 13／13 成功，旧 SDK 编译缺口已关闭，详见[CI 读回](ios-sdk-compatibility-2026-09-17.zh.md)。这不消除本次最大字号失败。计划仍为 28 完成／1 附条件／1 开发中／2 待开始，AGD-027 原性能要求、F13 暂缓及发布 No-Go 保持。
