# AGD-027：数据库升级后的四轮性能复验

日期：2026-09-16。源码基线：`2d8cc7aa62787cd466d3074b2c351e9df51820ce`。结论：**四轮为 5/6、4/6、5/6、6/6，原验收要求仍未全部通过。** 本记录不改写。用户同意后的新口径与当前源码四轮见[口径拆分](agd-027-budget-split-2026-09-22.zh.md)。

本批重新构建默认 SQLite 3.53.2 的开发与优化 CLI，冻结 247 项相同输入、两份二进制及一个四轮计划。计时前固定开发、优化、优化、开发的顺序；原验收仍要求两轮开发构建都通过六项，优化构建仅作对照。每路 5 次预热、30 次正式测量、1 KiB 正文、分位数算法和全部预算保持。没有用升级前网关的数据代表新版本。

## 全部结果

| 顺序／构建 | 原生读取 p95 ≤10 ms | 隔离读取 p95 ≤500 ms | 原生启动 ≤100 ms | 隔离启动 ≤2,000 ms | 通过 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1／开发 | 8.911 | 255.286 | 249.365 | 594.125 | 5/6 |
| 2／优化 | 18.082 | 404.195 | 546.550 | 355.918 | 4/6 |
| 3／优化 | 12.086 | 255.118 | 34.015 | 345.902 | 5/6 |
| 4／开发 | 8.907 | 262.381 | 43.158 | 359.966 | 6/6 |

八条受保护路由的采样网关峰值 RSS 为 13.250～14.047 MiB，均通过原 32 MiB 预算。四轮共 360 次正式读取、60 次预热，返回正文全部正确；不能只选最后一轮宣布通过。第一轮开发构建未过启动预算，两轮优化构建的原生读取仍超标。

所有轮次复用同一本地镜像，先以无挂载短命容器确认 Linux VM 已运行，实际唤醒及清理耗时单列。没有新增常驻容器、镜像或卷，结束后没有自有容器遗留。计时期间本任务未编译或运行其他测试；主机 load average 约从 3.3 变为 6.9，没有停止其他项目或系统服务。不能据此单独证明负载、签名检查或数据库检查点是本次失败根因。

## 核对与范围

- 独立核对冻结的 247 项输入、两份二进制、测量脚本和 VM 预检脚本摘要；从原始样本重新计算全部 24 项预算，确认计划在第一轮前冻结、四轮完整且顺序一致。
- 核对器拒绝删轮、改顺序、放宽预算、换候选、漏样本、错误字节数、非法时长、VM 未就绪、伪造全通过及计划轮数冲突，共十类反例。核对器成功只表示证据一致，输出中的 `performance_passed` 仍为 `false`。
- 仓库不变量 23 项、文档链接及公开 JSON 检查通过。固定 App 023 对应提交 `8b3418b` 的 GitHub CI 已读回 13/13 成功；CI 与本机性能预算分别计账。
- 本批为 CLI 性能复验，没有修改产品实现、审计同步参数或固定 App。固定 023 的 91 个文件与原清单保持；此前包内网关的五类真实数据库故障证据保持原候选身份。本次微基准正常完成后清理临时审计库，不冒充新增的逐行审计验链证据。
- macOS 启动诊断已单独提交；旧间歇性 CI 超时未因本批性能采样而宣布修复。此前首次启动耗时和旧性能失败继续保留。

原始证据及本批核对器位于 `.artifacts/m3-sqlite-performance-2026-09-16/`，公开摘要见[证据索引](evidence/m3-sqlite-performance-2026-09-16.json)。AGD-027 继续开发中；28／1／1／2 的任务计数、F13 暂缓、AGD-031 的外部需求条件和 AGD-032 发布 No-Go 保持。
