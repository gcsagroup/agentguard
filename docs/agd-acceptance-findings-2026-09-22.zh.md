# 统一验收发现（2026-09-22）

日期：2026-09-22。功能侧能在本机做完的部分已经做完：主动防御两波，以及 AGD-031 的本地 STIX 2.1 导入／导出。随后跑了软模式发布门禁（钉住 Rust 1.95.0，提交起点 `c0ce91f`，工作树含未提交改动）。

确认后已处理下面四项失败。F13、真机、公证和故意保留的边界没有动。发布仍是 No-Go。

## 门禁结果

自动检查 **10 通过 / 4 失败**，未验证 **12** 项，退出码 1。结论：不具备发布条件。

通过的自动项：工具链、clippy、离线评测、覆盖矩阵、能力主张映射、前端与 shell 解析、浏览器执行前阻断、macOS 编译、macOS 路径语义、部署自检基线。

## 已按确认处理的失败

1. **格式化。** 已跑 `cargo fmt --all`。`cargo fmt --all --check` 通过。
2. **供应链检查。** `cargo-deny` 0.20.2 已在 `~/.cargo/bin`，当时不在这次门禁的 PATH 里，所以被记成没装。补上 PATH 后第一次拉取 RustSec 顾问库时 GitHub TLS 失败；立刻重试后，根目录、macOS 壳和 Windows 壳都是 advisories、bans、licenses、sources 通过。壳子上有 `thiserror` 1.0／2.0 重复依赖警告，没有让检查失败。
3. **前端文件计数。** 看过 `guard-native-submit.js` 和 `native-submit.test.mjs`，没有 innerHTML、eval、setAttribute 这类 sink。常量从 31 改为 33。该测试通过。
4. **能力矩阵。** 已按源码重新生成三语文件。对应仓库不变量在 Rust 1.95 和 1.87 上都通过。

全仓 `cargo test --workspace` 和完整 MSRV 工作区测试没有重跑。上面只复验了当初失败的那两项不变量。

### 2026-09-23 源码整合补验

本次补跑完整工作区及 MSRV 1.87，各 1,551 项通过、17 项忽略；桌面 139 项通过、11 项忽略。扩展 E2E 实际 95 项通过（41 条基础、54 条实验性邮箱检查），界面 136 项通过，格式、严格 Clippy、三份依赖审计及实际 STIX CLI 往返通过。旧性能证据与当前源码不一致的拒绝保留，冻结副本复核通过；没有新的性能基准结论，也没有重跑完整发布门禁。分支清理和恢复路径见[整合记录](source-integration-cleanup-2026-09-23.zh.md)。固定 App 024、F13 和下列发布边界保持。

## 这次没有执行、因此不能算通过

| 项 | 原因 |
|---|---|
| F13 休眠／唤醒 | 会让这台 Mac 进入睡眠。没有在这次验收里执行 |
| `--strict` / `--ga` | 工作树不干净，且 12 类 RC 证据都没有环境变量。软模式已列出这 12 项。GA 另有 7 类（SBOM、隐私／商店、14 天 Beta、双 RC、五方签字、六渠道 smoke、5%→25%→100%）未跑 |
| 固定 App 024 | 未重建。桌面「明确注入后暂停同一会话网关」在源码里，不在正在使用的 024 包里 |
| 正式 Chrome 复测 `form.submit()` | 先前 B1／B2／B3／B5 手测的是第一波 ZIP。包装后的 `form.submit()` 只在 Chromium E2E 里通过，没有用新包装再手测 Chrome 153 |
| Edge B1–B5 | 本机没有 Edge |
| Android A1–A4、iOS 真机／TestFlight、Windows 真机、公证 | 门禁列为未验证，缺设备、证书或账号 |
| AGD-031 外部互操作 | 本地往返已通过（18 个手法、6 个案例、18 条缓解，补测的未知字段和悬空引用行为符合设计）。没有外部接收方，不能写成对方已收下 |

## 仍然故意不做的边界

页面从 iframe 恢复原生 `submit`、已发生的外部点击、视树差异升为 Critical、未核付款别名、Native Messaging、页内允许一次、IMPORT／PACKAGE／UNICODE 关键词，都不在本轮修复范围。
