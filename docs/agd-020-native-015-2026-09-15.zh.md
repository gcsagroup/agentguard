# AGD-020：固定 App 015 的权限保留、原生任务与接续验收

日期：2026-09-15。AGD-020 仍在实施中，F13 按用户要求暂缓、未验收。本批完成界面说明修正和同路径更新后的实际验证，不代表整个计划或发布通过。

## 固定入口与构建

日常入口仍是 `apps/desktop-macos/src-tauri/target/debug/bundle/macos/AgentGuard Local Agent Test.app`。名称、窗口标题、Bundle ID `com.agentguard.desktop.macos.localagenttest`、配置 `local-agent-20260910` 和组织签名身份保持不变。版本递增为 **Ver 2.0（015）**，基础版本 `2.0.0-dev.15`、`CFBundleVersion=15` 与三语页脚一致。

- App 可执行文件：`dc336f5f4de6a00b275c1bfe371432d608542251c70e505595f0ba24c10733ca`。
- 包内网关：`a011628363e6fdf75ac5910439ed60ebf7e5ecabe44fbff4479c8c71f40618a4`。
- 构建前冻结 1,074 个文件；核对 229 个网关及浏览器生产输入与 014 完全相同。因此复用 014 已冻结网关字节，**没有宣称重新编译网关**。本次重新编译和签名的是桌面 App。
- 014 的完整 91 文件归档保持不变；015 的当前包与新归档均核对完整文件清单。严格签名验证通过，指定签名要求与 014 相同；Gatekeeper 分发评估仍为退出码 3，未作外发公证通过声明。

原生前后对同一固定入口操作，没有再生成其他日常 App 名称。全局固定名称偏好已按用户要求保存，项目规则见 [AGENTS.md](../AGENTS.md)。

## 界面修正与权限实际效果

修正了三处容易误导的表达：

1. 权限未生效的帮助不再一概归因于未签名构建，改为核对当前名称、准确路径及各自的授权条目。
2. 两项权限都可用时，总览卡片显示“权限已就绪”；任一权限缺失时恢复排查说明。屏幕录制仍为可选，不提高启动门槛。
3. 采集停止后，开发状态行把保留的原始消息标为“上次采集结果”；原始错误仍可见，不用隐藏消息掩盖失败。

界面使用明确后端桩的交互测试 **129 项通过**，包含权限变更、撤回后的帮助恢复、旧启动消息的历史标识以及失败消息保留；这部分不作为真实授权证据。

实际打开 015 后，辅助功能和屏幕录制直接显示已授权，无需再次操作系统授权。实际开始观察时显示 `SCK=streaming(native=true,auto)`，原生帧产生 `OVL-007` 低对比度线索；未调用演示注入。结束观察后显示 `SCK=idle` 和“上次采集结果”，重复观察计数在之后保存并关闭合成文稿的期间保持 **296**。

观察会话为 `54918f0e-4edf-4933-84ae-fb7c9dbedf47`。本批状态快照显示 `AX=true`，返回 App 时显示自窗口跳过，**未把该快照计作 AX 正文读取证明**；014 已完成的真实 AX 读取证据保留在[前批权限记录](agd-020-native-014-permissions-2026-09-15.zh.md)。本批另实际退出 015、确认进程结束且文件未变，再打开同包，两项权限仍保留，总览为“未在守护”。

## 真实模型修复与宿主成果

新原生任务 `44710bc2-309b-491d-985c-e541b2fc52b9` 使用端口 8000 上已加载的 `Qwen3.6-35B-A3B-Uncensored-Heretic-MLX-4bit`。只提供预先声明的本机合成项目，没有安装依赖或启用浏览器。014 仅准备过表单，未启动此任务；真正执行、预览和回写均在 015 内完成。

初始实现按字符串排序且未去重，原四项测试中两项失败。模型读取三个输入文件，申请运行原测试、修复 `numbers.py`、再次运行同一组测试，并新增中文 `CHANGE.md`。两次 `python3 -B -m unittest -v` 都通过原生确认入口批准；首次失败结果保持真实失败，没有改记通过。

逐项展开两文件的修改前后全文，并滚动阅读变更说明末尾；独立核对快照摘要与预览一致、批准前宿主仍是原件。确认回写后 App 显示两文件“已应用”，宿主再次执行原四项测试 **4/4 通过**。`README.md` 和 `test_numbers.py` 内容及模式不变，原 `numbers.py` 的 644 模式恢复副本完整保存。

| 文件 | 回写后 SHA-256 |
| --- | --- |
| `numbers.py` | `6ba848ff26f6126e8777f88ab585d68ab4e856dd9f3a67fc33f6fadbcfbfcfe9` |
| `CHANGE.md` | `a2ad12b0c8161b1f4578ef0561b23843768c76fe0bfce7dafa2dcfbcf7f63342` |

停止会话后，控制连接文件移除，拥有的网关进程退出。新任务主日志 35 条、工具登记 3 条、模型出口 16 条，共 **54 条**逐条核对哈希链；旧来源库为空，迁移身份绑定另行核对。两个命令批准、回写动作与实际终态一致。原有 **29 个任务、1,027 条审计**的全部字段摘要保持不变。

## 同一网关的独立验收

下列后台验收使用与 015 包内相同的 `a011…` 网关。输出目录名称中的 `14` 保留启动时的候选记录，不因桌面更新重命名证据。

| 验收 | 本批结果 |
| --- | --- |
| 原工作区 T01～T50 | 50/50，逐任务输入、预览、宿主回写成果及单独报告一致 |
| 原工作区循环 | 100/100，10 类预声明流程，140 次连接实例；不替代 T01～T50 分母 |
| 真实 Chromium DOM | 前批同字节运行器 21/21；本批完成独立核对，886 条日志 |
| 本机真实模型建议与边界 | 9/9；正常、已知攻击、未知变体各 3；64 条日志 |
| DOM／模型联合核对 | 共 950 条，实际提交、拒绝、来源、文件与回答一致 |
| 核对器负例 | 重新计算主链后的迁移身份篡改、DOM 来源篡改及模型回答篡改均被拒；旧版日志正例仍通过 |

独立核对器现在识别来源与执行共用日志的格式，仍核对只读旧库身份、链头、完整导入前缀、来源唯一性和父链。没有通过放宽来源要求来迁就新格式。

最终仓库不变量 **23 项通过**，五个修改的 JavaScript 文件语法检查、证据摘要及当前／归档两份 91 文件 App 清单核对通过。

上一提交 `b21b73534b91afb4184ad41d4ccd75f609c3cf47` 的 GitHub CI 已完成 **13/13**，见 [运行 34985241161](https://github.com/gcsagroup/agentguard/actions/runs/34985241161)。本批提交的 CI 另行跟踪。

## 剩余工作及证据入口

已启动同一冻结网关的原定义 30 分钟持续运行，本文写入时仍在进行，未计通过。签名规则联合、浏览器完整任务及循环、其余受影响 M1 故障回归继续。当前为 18 项完成、1 项附条件完成、1 项实施中、12 项待开始；AGD-021 之后仍按依赖推进。

结构化索引见 [015 证据](evidence/m2-native-015-2026-09-15.json)。原始构建、测试和核对脚本位于 `.artifacts/m2-acceptance-close-2026-09-15/`；014 的性能与入口原始记录继续留在前批目录。F13、完整原 M1、全平台发布与整个开发计划的未完成边界不变。
