# AGD-027：新数据库候选的联合功能复验

日期：2026-09-16。本批基线为 `672d65d3f578a8e60986ab4c51eb27c7a4d49876`。**工作区持续运行、记忆／RAG、委托预算／传播、浏览器、隔离及 F14 已按原分母复验通过；原性能验收仍未通过，AGD-027 继续开发中，发布保持 No-Go。**

## 候选及执行方式

继续使用[恢复复验](agd-027-sqlite-recovery-2026-09-16.zh.md)的同一份冻结 SQLite 3.53.2 开发网关，SHA-256 为 `b5fdf2cab601610bcd7573b3719b868f0fa5f6a7d5915e8649949dae2a9193e0`。原 254 项生产及核对输入保持，另在运行前冻结本轮 16 项脚本／矩阵输入和原生包内浏览器运行时摘要。未重新构建或用历史网关替代本候选。

原 30 分钟工作区父流程正常退出后，先独立核对全部四阶段，再按冻结计划串行启动后续流程。所有场景复用同一既有 Docker 镜像；F14 只启动既有、本项目标记且原来已停止的合成服务，结束后再次确认已停止。

## 完成结果

| 范围 | 实际执行与独立核对 |
| --- | --- |
| 工作区原分母 | 50 个完整任务、100 次循环、50 项故障及 30 分钟持续运行全部通过。持续运行完成 30 个任务和 121 次心跳，实测 30.02 分钟，最长间隔 15.41 秒，无自有容器遗留。成果与范围见恢复记录。 |
| 记忆／RAG | 4 个真实网关进程、3 个新模型任务；拒绝、批准、重新读取、撤销与过期结果正确。独立核对 5 条签名记忆记录、4 个批准版本和 89 条执行审计。 |
| 持久资料与委托传播 | 6 个真实网关进程、2 个新模型任务、2 次实际文件投递；接收后的两次再次触发均拒绝，撤销后新任务中不再出现资料标记，正常偏好保留。独立核对 6 条记忆记录、162 条网关审计及 33 个签名。 |
| 委托预算 | 6 进程、13 份签名授权、15 条已认证消息、33 个签名和 113 条审计核对通过；16 次预留、15 次结束，无法确认的结果按全部预留扣账。原金额不可测边界保持。 |
| 浏览器正常与循环 | B01～B10 全部完成；100 次循环包含 50 次批准、40 次拒绝、10 次断连。服务端实际收件恰好 50 次，独立核对 1,225 条浏览器审计与持久执行回执。生命周期对照及清理检查通过。 |
| 隔离 | 原 78 项全部通过，覆盖正常读写、范围外读写、链接、子进程、宿主环境／控制文件／Docker socket 不可见等实际检查。 |
| F14 虚拟网卡 | 保持同一服务进程，实际移除／恢复 Linux VM 的网卡；已发出请求变为未知，恢复后至少 25 秒无重发，旧批准和旧会话恢复均拒绝；新会话独立批准后只增加一次收件。33 条审计与实际请求正文核对一致。 |

浏览器运行日志中的 13 项测试包含循环和生命周期测试，不能将“13 项”写成 13 个完整用户任务。两组模型任务共使用本机 `Qwen3.6-35B-A3B-Uncensored-Heretic-MLX-4bit`；输入为合成资料，批准由独立宿主脚本完成，模型输出没有被自动执行。CLI 记忆夹具显式使用明文测试模式；签名核对不等于加密验收，SQLCipher 证据另列。

## 负面对照与证据

独立核对器从实际 SQLite 读取浏览器审计，再与批准、回执、服务端收件逐项比对。六类内存变异均被拒：删完整任务、改收件正文、同时篡改摘要及分项回执、隐藏网卡故障的未知结果、删隔离检查、换网关摘要。原报告和数据库保持；这证明这些不一致可检测，不宣称能对抗同时控制全部原始证据的人。

13 个串行执行／核对阶段全部退出 0；同一源码基线的 [GitHub CI 35093240211](https://github.com/gcsagroup/agentguard/actions/runs/35093240211) 完成 13／13。运行后再次核对 254 项输入及固定 App 023 的 91 个文件／模式，均保持。未新增日常 App、重新签名或调整授权；当前文件核对不替代新的原生操作验收。

原始计划、冻结脚本、进程退出、模型请求／响应、独立核对及浏览器数据库位于 `.artifacts/m3-sqlite-recovery-2026-09-16/joint/`。另在 `retained-audits/` 留存 10 份数据库、公钥和见证副本，逐字核对与已结束流程的原件一致；原合成夹具保留，私钥不进入 Git。公开摘要见[证据索引](evidence/m3-sqlite-joint-2026-09-16.json)。

整理副本时首次将委托配置误按记忆数据库配置解析，返回 `KeyError: 'database'`；错误摘要保留。按两种实际配置结构分别处理后，10 份副本全部核对通过，没有更改产品结果或重跑验收。

## 仍未完成

本批没有重测性能，没有将功能测试后的启动用作首次启动成绩。[原四轮性能](agd-027-sqlite-performance-2026-09-16.zh.md)的 5/6、4/6、5/6、6/6 及[入口前等待诊断](agd-027-startup-phases-2026-09-16.zh.md)继续保留。启动预算未获范围澄清前，仍按严格口径判未通过；没有降低持久同步、预热后选样或修改系统安全策略。

F14 仅覆盖 Docker Linux VM 网卡，Mac 入口仍是回环；F13 暂缓且未验收。各平台签名／真机与正式发布材料、AGD-031 的外部共享需求条件均保持。任务计数仍为 28 完成、1 附条件完成、1 开发中、2 待开始，不能把本批功能复验等同于完整 M3 或发布验收。
