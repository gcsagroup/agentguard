# AGD-020：012 候选 M1 回归与性能退化

日期：2026-09-15。**AGD-020 仍在实施，性能回归未通过。** 原定义 30 分钟持续运行、安全负例、隔离、显式故障、出口与浏览器恢复通过；固定路径 012 App 完成一次真实模型修复、全文预览及实际回写。性能沿用原预算，仅 4/6 通过，不能进入依赖 AGD-020 完成的后续验收。

## 同一候选与完整分母

全部网关实测使用冻结的 Ver 2.0（012）网关 `948d5d25109a21b93e25317054868d74c572af310c985c0ce546bd2c51dee97d`。1042 个冻结源文件再次核对；本批没有修改产品实现或重建 App。沿用原 Docker 镜像 `7de5789d…`，各套 Docker 验收串行运行，没有增加镜像、卷或常驻测试容器。

| 验收 | 实测结果 | 范围 |
| --- | --- | --- |
| 原 30 分钟持续运行 | 1801.12 秒、30/30 任务、121 次心跳 | 独立会话全程存活，最长心跳间隔 15.24 秒；包括待批准和执行中 SIGKILL、真实 30 秒超时及恢复 |
| 工作区安全负例 | 50/50 | 操作者身份、旧预览、宿主冲突、路径与权限等原分母 |
| 原隔离套件 | 78/78 | 真实快照、路径、网络、子进程与恢复 |
| 显式工作区故障 | 8/8 | 同源码 Server 测试程序，实际 Docker、回写与 SQLite；不冒充 CLI 测试 |
| 宿主出口控制 | 42/42 | 真实本机 HTTP、凭据边界、撤权竞争、超时和持久恢复 |
| 容器出口探针 | 24 次隔离尝试均未到达 | Node／Python 各含主进程与后代，分别测试 6 条网络路径 |
| F08 浏览器崩溃 | 通过 | 本次私有 Chromium 待提交时真实终止；零旧 POST、旧批准 409，新会话明确批准后单次 POST |
| 浏览器系统解析与私有数据 | 通过 | 生产沙箱中的探针不能解析、直接 TCP／UDP、读取私有夹具或继承合成凭据 |

持续运行已从原执行句柄收齐正常退出，未重启、缩短或用循环数量代替时长。它的 30 个任务不加入原 T01～T50 分母。[前批工作区](agd-020-independent-inputs-2026-09-15.zh.md)和[DOM、模型及浏览器](agd-020-dom-model-2026-09-15.zh.md)结果分别保留。

容器探针先在宿主和同镜像默认网络进行正向校准，再经真实网关在断网容器中执行；7 类接收计数增量全部为零，合成私有文件、Unix socket 和宿主环境均不可用。IPv4 映射 IPv6 有正向校准；`::1` 只覆盖容器回环，没有声称验证原生外部 IPv6 网络。默认网络仅用于校准自有接收器，不是产品授权。

## 真实原生回写

固定路径仍为 `apps/desktop-macos/src-tauri/target/debug/bundle/macos/AgentGuard Local Agent Test.app`，本地身份和数据环境未换。复用已加载的本机 35B 模型，对新建的无敏感数据排序项目执行真实工具操作。原 3 个测试中先有 1 个失败；模型读取 3 个文件，运行失败测试，修复 `sorted(values, key=str)` 为 `sorted(values)`，重跑测试并新增中文说明，共 7 个实际工具步骤。

通过原生界面展开两个文件的修改前后全文，核对目标与恢复目录，再点击确认回写。界面显示两项已应用，随后从宿主实际运行原测试 **3/3 通过**；原测试、README 摘要不变，恢复目录中的旧排序文件与初始摘要一致。模型最初只改副本，原件实际变化来自这次人工回写。完成后通过界面断开并停止，控制连接文件消失。

新任务 27 条审计独立验链并与导出的逐行 JSON 一致；全部 28 个原始任务及 993 条旧审计保持不变。原生任务卡部分内容仍未出现在自动化无障碍树中，本轮通过窗口截图和可见控件操作完成；该兼容性问题未改写成读屏通过。这是一个新增专项流程，不是完整 M1 原生矩阵再次通过。

## 保留的性能失败

正式测量前复制原预算，以 Linux VM 启动身份核实已运行，测量前后没有换 VM。每路预热 5 次、正式读取 30 次，输入仍为 1 KiB，三个路径全部内容正确；下表的启动值各只有一次样本，不是启动 p95。

| 门槛 | 012 实测 | 原预算 | 结果 |
| --- | --- | --- | --- |
| 本机网关读取 p95 | 19.99 ms | ≤ 10 ms | 未通过 |
| 本机网关启动 | 1316.54 ms | ≤ 100 ms | 未通过 |
| 本机网关采样 RSS | 17.34 MiB | ≤ 32 MiB | 通过 |
| 隔离网关读取 p95 | 295.07 ms | ≤ 500 ms | 通过 |
| 隔离网关启动 | 1896.13 ms | ≤ 2000 ms | 通过 |
| 隔离网关采样 RSS | 17.88 MiB | ≤ 32 MiB | 通过 |

没有调整预算或把内容读取成功写成性能通过。后续对同一冻结二进制单独使用 macOS `sample` 诊断：启动采样集中在内建工具包计算当前可执行文件 SHA-256 的软件实现；读取采样集中在审计与来源记录的 SQLite 提交及 `full_fsync`。诊断运行有采样开销，不能替代上表正式实测，也没有据此声称修复完成。

下一步先优化可执行文件摘要路径，再评估保持持久化及执行前记录边界的事务改进；修复后冻结新候选、递增 App 构建号并复验受影响范围。不能为性能关闭持久化保证。

## 独立复核与 CI

[复核器](../scripts/acceptance/verify-m2-regression.py)重新计算原始 30 个样本的 p95，对比全部 6 项预算、逐任务持续运行记录、心跳连续性、容器接收数据和新任务原始审计。输出明确为 `evidence_verified=true`、`regression_passed=false`。把预算失败篡改为通过，或改动一条私有审计副本，两种负例均被拒绝；回收后的临时服务和文件只能复核运行时记录，不称再次实测。

本机完整桌面测试 128 项、另一次调度专项 20 项、仓库不变量 23 项通过；Node／Python 语法、两份 JSON 与 38 个本地文档链接检查通过。[上一提交 1663dc9 的 CI](https://github.com/gcsagroup/agentguard/actions/runs/34955811655) 为 12/13 作业通过：macOS 作业有 3 项合成模型测试在 8 秒启动等待时超时，本机未复现。原失败日志保留，原因尚未证实；请求重跑该失败作业被 GitHub 以缺少管理员权限拒绝，未创建空提交来反复重试。本批正常提交后的 CI 另行核对。

012 固定路径与归档的 91 个 App 文件、严格签名和原有 127 个独立文件再次核对一致。原始证据位于 `.artifacts/m2-regression-2026-09-15/`，持续运行位于 `.artifacts/m2-dom-model-2026-09-15/soak-01/`；公开摘要见[结构化索引](evidence/m2-regression-2026-09-15.json)。[容器出口脚本](../scripts/acceptance/agd-container-egress.mjs)已从原 E01 探针整理为可复用入口，正式运行需显式网关路径、摘要、Python 和新输出目录。

F13 继续暂缓、未验收，F14 保留已经通过的 Docker 虚拟链路范围。本轮不是因 F13 停滞，而是发现可继续修复的性能退化；累计仍为 18 项完成、1 项附条件完成、1 项实施中、12 项待开始，整个计划未完成，发布仍为 No-Go。
