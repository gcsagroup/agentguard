# Chrome 正式版验收准备与未完成记录

日期：2026-09-17。源码为 `c56fd022893f728d40e1ca103d4ed9bf472af482`；该提交的 [CI 35123521270](https://github.com/gcsagroup/agentguard/actions/runs/35123521270) 已终态成功，13 项作业全部通过。**本轮没有新增 Chrome／Edge 原生验收通过项，发布仍为 No-Go。**

## 冻结包与环境

沿用同一路径 `apps/extension-chromium/dist/agentguard-extension.zip` 打包，先保留原本地 ZIP。新包 SHA-256 为 `67b34bbedbb77bed07c5cb6ae99b11a7ad12b3a53650496cce686bde6e2e49e2`，manifest 为 `1.0.0.1`／`1.0.0-rc.1`。23 个普通文件分别与当前源码和解压副本逐字节核对；权限不含 `nativeMessaging`，没有 Native host。这是准备验收的共用 ZIP，尚非六端冻结发布集。

实际 `/Applications/Google Chrome.app` 及新进程读回版本均为 `152.0.7977.84`。本轮未检测到可用的 Microsoft Edge，未安装新浏览器。GitHub Release 列表为空；这不能证明扩展从未在商店发布，上一公开版本的来源仍需提供或确认。

## 实际尝试与边界

测试路径为：空白专用资料 → `chrome://extensions/` → 开发者模式 → 加载解压后的冻结 ZIP。使用前端测试调试技能；当前未列出 Browser 插件／技能，因此使用既有 Playwright，同时尝试 Computer Use 原生窗口控制。

专用进程的空白扩展页确实渲染，截图和可访问内容已核对。点击加载后，Playwright 未收到文件选择器事件，5 秒超时；原生控制工具按应用标识及路径取得的都是日常 Chrome 实例，无法定位该独立进程。此次尝试保持原生加载流程，未安装扩展。

随后关闭专用进程，通过日常 Chrome 的“添加 Chrome 个人资料”入口尝试建立验收资料。新窗口只返回 `Google Chrome` 标题，没有可访问控件，截图接口也不可用。Escape 未退出；关闭本轮新开窗口后已回到原窗口。没有在日常资料中加载 AgentGuard，没有登录账号，也没有完成新资料创建。专用进程退出码为 0，所建空白资料已可恢复归档，原路径映射保存。

| 原验收项 | 本轮状态 |
| --- | --- |
| B1 安装及包身份 | ZIP 身份及权限已核对，但实际加载未完成，整体 BLOCKED。 |
| B2 引导、面板与规则集 | 扩展未安装，未执行。空白扩展管理页不是产品界面通过证据。 |
| B3 阻断及正常行为 | 未执行；此前 Chromium 自动化和 CI 不能替代本项 Chrome 正式版验收。 |
| B4 上一公开版本升级 | 缺少已确认的上一公开版本；不得用本地旧 ZIP 冒充公开版本。 |
| B5 禁用、重启、卸载和回滚 | 未执行；本轮进程清理不是扩展生命周期验收。 |
| Edge B1–B5 | 没有可用浏览器，未执行；不得复制 Chrome 结果。 |

## 验证与接续

既有打包回归通过：完整替换旧 ZIP、内容匹配、GA 权限及 Firefox 排除门禁均保持。首次直接执行测试脚本因其没有可执行位返回 126，随后用其既有 Bash 入口成功；没有改权限或覆盖失败日志。仓库不变量 23 项通过，文档链接与原始产物摘要核对通过；固定 macOS App 024 的 91 个文件和模式逐项核对不变。

继续原生验收需要能控制干净 Chrome 资料中的原生目录选择器，并准备正式 Edge；B4 另需真实上一公开版本。现有活动时间线、macOS 权限与固定 App 身份不因此重新打包或重新申请权限。AGD-027 性能、F13 暂缓、AGD-031 条件与 19 类正式发布证据要求保持。

原始包、截图、接口失败、资料归档和清理记录位于 `.artifacts/chrome-native-2026-09-17/`，公开摘要见[证据索引](evidence/chrome-native-preparation-2026-09-17.json)。本记录保留未完成状态，不产生正式 `PASS (native)` 或发布证据 JSON。
