# AGD-027：性能口径拆分与当前源码四轮复验

日期：2026-09-22。源码基线：`c0ce91f52b4ac4a309873058e8f49124a44c27ec`。状态：**新口径下四轮均通过，两轮开发构建硬门槛全部通过。** 原 100 ms 首次启动失败记录保留，不改写；未降低 `synchronous=FULL`，未重建固定 App 024。

用户已同意：签名环境下的首次启动改为已记录限制；原生读取 p95 按现网关重冻为 ≤20 ms。本批只改核对口径并重跑微基准，没有把旧 10 ms／100 ms 计划改成通过。

## 口径

| 项 | 原硬门槛 | 本批 |
| --- | --- | --- |
| 原生读取 p95 | ≤10 ms | ≤20 ms |
| 隔离读取 p95 | ≤500 ms | 不变 |
| 原生首次启动 | ≤100 ms | 只记录，`maximum` 为 `null` |
| 隔离启动 | ≤2,000 ms | 不变 |
| 网关 RSS | ≤32 MiB | 不变 |
| 持久提交 | `FULL` / `fullfsync` | 不变 |

通过条件仍是两轮开发构建的硬门槛全部通过；优化构建只作同源码对照。首次启动实际值写入检查表，不能删样本或改系统安全策略冒充冷启动达标。若改回 `native_startup_ms: 100`，核对器拒绝本批计划。

## 四轮结果

冻结 250 项构建输入。顺序为开发、优化、优化、开发。每路 5 次预热、30 次正式测量、1 KiB 正文。420 次读取正文全部正确。

| 顺序／构建 | 原生读取 p95 ≤20 ms | 隔离读取 p95 ≤500 ms | 原生启动（记录） | 隔离启动 ≤2,000 ms | 硬门槛 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1／开发 | 8.140 | 328.850 | 655.609 | 348.864 | 5/5 |
| 2／优化 | 9.082 | 337.249 | 559.946 | 433.676 | 5/5 |
| 3／优化 | 8.558 | 300.986 | 35.721 | 414.598 | 5/5 |
| 4／开发 | 8.988 | 277.447 | 36.970 | 461.607 | 5/5 |

网关采样峰值 RSS 为 13.500～14.141 MiB。第 1、2 轮原生启动仍超过原 100 ms；按新口径不计入失败。第 3、4 轮重复启动为 35.7／37.0 ms。不能用后两轮代替首次启动测量。

开发二进制 SHA-256 `f5adcd850f6d1de21fa10a3806fb7ab2027bc35f18d9b5b4b4ba7ec7652a6866`；优化二进制 `8b03ea2de9ae04e07a88c98d6a8df160b382f0c34b2f7f38c37e5a8c49e325e6`。计时前已复制，计时期间本任务未再编译。沿用既有 Docker 镜像，预检确认 Linux VM 就绪。

## 核对与范围

独立核对器从原始样本重算全部 24 项，核对 250 项输入、两份二进制、基准脚本和冻结计划。十一类反例均拒绝，包括把首次启动改回 100 ms 硬门槛、把读取预算改回 10 ms、删轮、换顺序、伪造全通过。核对命令：

```bash
python3 scripts/acceptance/verify-m3-budget-split.py \
  .artifacts/m3-budget-split-2026-09-22
```

本批未重跑 50 个任务、100 次循环、30 分钟运行、浏览器或 F14；那些功能分母仍属[SQLite 升级后联合复验](agd-027-sqlite-joint-2026-09-16.zh.md)的原候选身份。[原 10 ms／100 ms 四轮](agd-027-sqlite-performance-2026-09-16.zh.md)及[最小进程首次入口](agd-027-startup-control-2026-09-17.zh.md)继续保留。固定 App 024 未重建、未改签名、未重绑权限。

功能分母已在先前 SQLite 网关上通过，性能是 AGD-027 剩下的开发中项。按用户同意的口径，该项关闭。F13 仍暂缓，完整原 M1 与发布仍为 No-Go。

原始计划、日志、二进制和核对器位于 `.artifacts/m3-budget-split-2026-09-22/`，公开摘要见[证据索引](evidence/m3-budget-split-2026-09-22.json)。
