# 本轮候选改进与接入验收要求

本轮无新策略版本。当前源码的依赖和网关已变化，因此在新冻结快照复验5个候选族；正式源码未修改。原27项策略输入完整保留，新增4项正常查询改写。31项中7项正常查询误拒绝，拒绝的是当前候选版，线索继续保留。

IMPORT/PACKAGE：SafeShell::evaluate 在字面工具放行前调用 denied_operand；Gate::judge_with_content 又先调用 shell.evaluate，后续内容上下文无法挽回提前拒绝。现有全局YAML字段不能同时保留执行参数限制并只对可信字面查询区分处理。本轮不删拒绝词、不放宽预期，不以无依据的第二、三版凑通过。

接入点是 crates/guard-shell/src/lib.rs::evaluate/denied_operand/find_injection、crates/guard-gateway/src/gate.rs::judge_with_content 及服务端实际工具分派。工具类别必须由宿主和 ToolRegistry::verify 提供，不能信任正文或第三方自报只读。验收须原31项全保留，追加伪造同名工具、参数类别变更、普通授权升级、正常查询完成和拒绝后无副作用。现有 tool_registry.rs 的登记校验可复用，但本轮没有证明它与上述参数语义已经完整接通。

UNICODE：原9项仍有论文引用阻断、标签研究标记告警两项失败。接入点是 guard-core 文本异常检测、guard-gateway/content.rs 的可信来源视图及 gate.rs。需要分别验证展示/引用和执行授权；不得从一段文字自报研究用途推导放行。保留原文、规范化视图及合法旗帜、普通多语和旧失败样本，设计未实施，不计通过。

MEMORY：PRIV-004 的12项引擎确认对照通过；当前已存在 guard-gateway/src/memory.rs::MemoryRuntime 与持久记忆实现，不能笼统写成产品完全没有记忆能力。仍需针对指定真实客户端验证写入必经入口、可信事件、内容/目标/单次批准绑定、临时存储的跨会话读取与拒绝后无写入。本次没有验证这些接入或读取用户真实记忆。

EGRESS：14项本机组件对照及42项既有出口回归通过。仍需在真实受保护会话验证所有出口经受控路径、直接socket不可绕过、DNS/代理/逐跳目的地与撤销生效；现有隔离和出口实现应先核对再补缺口。不得由本机组件结果推出第三方MCP认证或全部环境出口受控。

AUTH、TENANT、OPAQUE本轮未深入重验，旧失败与接入需求保留。缺少产品能力的部分须另有修改源码的授权，本自动化只交付候选与验收要求。
